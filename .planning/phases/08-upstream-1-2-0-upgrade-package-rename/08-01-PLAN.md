---
phase: 08-upstream-1-2-0-upgrade-package-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - CLAUDE.md
  - src/cli.ts
  - src/postinstall.ts
  - .github/workflows/publish.yml
  - tests/cli.test.ts
  - docs/PUBLISHING.md
  - scripts/postinstall.mjs
autonomous: true
requirements:
  - UPSTREAM-01

must_haves:
  truths:
    - Project package name reads `pi-gsd-core` in package.json name/bin/description fields
    - No occurrence of `pi-gsd-redux` remains in src/, tests/, docs/, README.md, CLAUDE.md, package.json, .github/, or scripts/ (excluding intentional CHANGELOG/history mentions)
    - `npm run check` is green at commit tip
    - Commit message body ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
  artifacts:
    - path: package.json
      provides: "Renamed package metadata and bin entry"
      contains: '"name": "pi-gsd-core"'
    - path: src/cli.ts
      provides: "CLI usage string updated"
      contains: 'Usage: pi-gsd-core'
    - path: .github/workflows/publish.yml
      provides: "CI publish workflow references new package name"
    - path: tests/cli.test.ts
      provides: "Test assertions match renamed CLI"
      contains: 'Usage: pi-gsd-core'
    - path: docs/PUBLISHING.md
      provides: "Runbook references new package name"
      contains: 'pi-gsd-core'
  key_links:
    - from: package.json
      to: src/cli.ts
      via: "bin entry name matches CLI usage header"
    - from: src/cli.ts
      to: tests/cli.test.ts
      via: "usage string assertion stays in sync"
---

<objective>
Rename the project from `pi-gsd-redux` to `pi-gsd-core` across all package metadata, CLI surfaces, documentation, CI configuration, test assertions, and publishing runbook. The GitHub repository URL (`pi-gsd`) stays unchanged.

Purpose: Align the adapter name with the upstream `@opengsd/gsd-core` package family (D-29).
Output: All project identifiers reflect the new name; `npm run check` passes.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-CONTEXT.md
@.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename project identifiers in package.json, CLI, docs, CI, tests, and runbook</name>
  <files>package.json, README.md, CLAUDE.md, src/cli.ts, src/postinstall.ts, .github/workflows/publish.yml, tests/cli.test.ts, docs/PUBLISHING.md</files>
  <read_first>
    - src/cli.ts:22 (usage string containing `pi-gsd-redux`)
    - src/postinstall.ts:35,53,55,60,78,80,85 (self-reference strings)
    - package.json (name, bin, description fields)
    - README.md (title and install commands)
    - CLAUDE.md (title and prose references)
    - .github/workflows/publish.yml (any package name reference)
    - tests/cli.test.ts:258 (assertion `expect(stderr.join("")).toContain("Usage: pi-gsd-redux")`)
    - tests/cli.test.ts:293 (symlink fixture name `linked-pi-gsd-redux`)
    - docs/PUBLISHING.md (install commands, npm publish examples, CLI usage references)
  </read_first>
  <action>
    Update every occurrence of `pi-gsd-redux` to `pi-gsd-core` in the listed files, EXCLUDING:
    - CHANGELOG/history mentions of past releases
    - Repository URL slug (`pi-gsd` stays)

    Specific changes per file:
    - package.json: `"name": "pi-gsd-core"`, `"bin": { "pi-gsd-core": "dist/cli.js" }`, update description if it contains old name, update files array if it lists README/ docs paths
    - src/cli.ts: Update `usage` string at line 22 from `Usage: pi-gsd-redux` to `Usage: pi-gsd-core`
    - src/postinstall.ts: Update all log message prefixes from `pi-gsd-redux postinstall:` to `pi-gsd-core postinstall:`
    - scripts/postinstall.mjs: Update header docblock references (`Postinstall script for pi-gsd-redux`, `pi install npm:pi-gsd-redux`) and all `pi-gsd-redux postinstall:` log message prefixes (lines ~44, ~47, ~62, ~66) to `pi-gsd-core`. Postinstall script MUST still exit 0 on all paths per CLAUDE.md D-10 — verify no behavior change.
    - README.md: Update title, all `pi install npm:pi-gsd-redux` → `pi install npm:pi-gsd-core`, all `npm install -g pi-gsd-redux` → `npm install -g pi-gsd-core`, all `npx pi-gsd-redux` → `npx pi-gsd-core`
    - CLAUDE.md: Update title/reference paragraphs from `pi-gsd-redux` to `pi-gsd-core` (keep upstream `@opengsd/get-shit-done-redux` references untouched — those are upstream package name, not project name)
    - .github/workflows/publish.yml: Update any package name reference if present
    - tests/cli.test.ts: Update line 258 assertion (`expect(stderr.join("")).toContain("Usage: pi-gsd-redux")` → `"Usage: pi-gsd-core"`); update line 293 symlink fixture name (`linked-pi-gsd-redux` → `linked-pi-gsd-core`); search the rest of the file for any other `pi-gsd-redux` literal and update
    - docs/PUBLISHING.md: Find-and-replace `pi-gsd-redux` → `pi-gsd-core` throughout (install commands, package name in npm publish examples, CLI usage in runbook); preserve any historical commit hashes or git URLs that reference the old project name only if they are truly historical artifacts (when in doubt, replace)
  </action>
  <verify>
    <automated>
      grep -r "pi-gsd-redux" src/ tests/ docs/ README.md CLAUDE.md package.json .github/ | grep -v "CHANGELOG" | grep -v "history" | wc -l
      # Expected: 0
    </automated>
  </verify>
  <done>No `pi-gsd-redux` occurrences remain in active code/docs (excluding historical CHANGELOG mentions)</done>
  <acceptance_criteria>
    - `grep -r "pi-gsd-redux" src/ tests/ docs/ README.md CLAUDE.md package.json .github/ scripts/ | grep -v "CHANGELOG" | grep -v "history"` returns empty
    - `grep -n "pi-gsd-redux" tests/cli.test.ts` returns 0 matches
    - `grep -n "pi-gsd-redux" docs/PUBLISHING.md` returns 0 matches
    - `grep -n "pi-gsd-redux" scripts/postinstall.mjs` returns 0 matches
    - `npm test tests/cli.test.ts` exits 0
    - `npm run check` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify build and check gate after rename</name>
  <files>dist/cli.js (build output)</files>
  <read_first>
    - package.json (scripts.check to confirm gate command)
  </read_first>
  <action>
    Run `npm run check` (typecheck + test + build + doctor). If failures relate to the rename (e.g., hardcoded bin name in tests), fix them within this plan's file set. Do NOT modify upstream-package references (`@opengsd/get-shit-done-redux`) — those belong to Plan 08-02.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>`npm run check` passes with renamed project identifiers</done>
  <acceptance_criteria>
    - `npm run check` command exits 0
    - `node dist/cli.js doctor --prompts generated/prompts --agents --cwd .` output contains `pi-gsd-core` (not `pi-gsd-redux`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
N/A — pure code migration; no new attack surface introduced. Existing surfaces (postinstall, Pi extension hooks) preserve their CLAUDE.md hard rules (never fail / never throw).
</threat_model>

<verification>
- `grep -r "pi-gsd-redux" src/ tests/ docs/ README.md CLAUDE.md package.json .github/ scripts/ | grep -v "CHANGELOG" | grep -v "history"` returns empty
- `npm run check` green
</verification>

<success_criteria>
- Project name is `pi-gsd-core` in package.json, bin entry, CLI usage, README, CLAUDE.md, CI workflow, test assertions, and publishing runbook
- Zero accidental `pi-gsd-redux` strings in active source/docs
- `npm run check` green at commit tip
- Commit message ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
</success_criteria>

<output>
Create `.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-01-SUMMARY.md` when done
</output>
