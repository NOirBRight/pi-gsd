---
phase: 08-upstream-1-2-0-upgrade-package-rename
plan: 02
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - src/official.ts
  - package.json
  - src/rewrite-workflow-paths.ts
  - src/gsd-models.ts
  - src/prompt-transform.ts
  - tests/fixtures.ts
  - tests/agent-sync.test.ts
  - tests/agent-transform.test.ts
  - tests/cli.test.ts
  - tests/doctor.test.ts
  - tests/e2e/doctor-smoke.test.ts
  - tests/e2e/generate-smoke.test.ts
  - tests/gsd-models-*.test.ts
  - tests/official-resolver.test.ts
  - tests/prompt-transform.test.ts
  - tests/rewrite-workflow-paths.test.ts
autonomous: true
requirements:
  - UPSTREAM-01
  - UPSTREAM-02

must_haves:
  truths:
    - `OFFICIAL_PACKAGE_NAME` in src/official.ts reads `"@opengsd/gsd-core"`
    - package.json dependency entry is `"@opengsd/gsd-core": "1.2.0"` (old entry removed)
    - `node -e "require.resolve('@opengsd/gsd-core/package.json')"` succeeds
    - `grep -rn "@opengsd/get-shit-done-redux" src/ tests/` returns no matches
    - `npm run check` is green at commit tip
    - Commit message body ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
  artifacts:
    - path: src/official.ts
      provides: "Canonical upstream package name constant"
      contains: 'OFFICIAL_PACKAGE_NAME = "@opengsd/gsd-core"'
    - path: package.json
      provides: "1.2.0 dependency declaration"
      contains: '"@opengsd/gsd-core": "1.2.0"'
    - path: tests/fixtures.ts
      provides: "Test fixtures reference new package name"
      contains: '"@opengsd/gsd-core"'
  key_links:
    - from: src/official.ts
      to: tests/fixtures.ts
      via: "OFFICIAL_PACKAGE_NAME constant consumed by fixture builder"
    - from: package.json
      to: node_modules/@opengsd/gsd-core
      via: "npm install resolves 1.2.0"
---

<objective>
Migrate all upstream package references from `@opengsd/get-shit-done-redux@1.1.0` to `@opengsd/gsd-core@1.2.0`. Update the canonical constant, dependency declaration, test fixtures, inline documentation comments, and install the new package. Verify the `sdk/shared/model-catalog.json` path still resolves in 1.2.0.

Runs in wave 3 after 08-03 has removed all code that depended on the upstream SDK directory (`sdk/dist/query/*.js`), allowing a clean swap from 1.1.0 → 1.2.0 without intermediate broken state.

Purpose: Decouple from the retired package name and establish the 1.2.0 surface as the new canonical upstream (D-12 pin lifted, UPSTREAM-01).
Output: All code, tests, and comments reference `@opengsd/gsd-core`; `npm run check` green.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-CONTEXT.md
@.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md
@src/official.ts
@src/rewrite-workflow-paths.ts
@src/gsd-models.ts
@src/prompt-transform.ts
@tests/fixtures.ts
@tests/prompt-transform.test.ts
@tests/official-resolver.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OFFICIAL_PACKAGE_NAME constant and dependency, install 1.2.0</name>
  <files>src/official.ts, package.json</files>
  <read_first>
    - src/official.ts:5 (`OFFICIAL_PACKAGE_NAME` constant)
    - package.json (dependencies block with `@opengsd/get-shit-done-redux` entry)
  </read_first>
  <action>
    1. In src/official.ts: change `export const OFFICIAL_PACKAGE_NAME = "@opengsd/get-shit-done-redux"` to `"@opengsd/gsd-core"`.
    2. In package.json: remove the `"@opengsd/get-shit-done-redux": "1.1.0"` dependency entry entirely. Add `"@opengsd/gsd-core": "1.2.0"` in its place.
    3. Run `npm install` to resolve the new package.
    4. After install succeeds, run `node -e "require.resolve('@opengsd/gsd-core/package.json')"` to verify module resolution.
    5. Run `node -e "require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')"` to verify the CLI bridge path exists in 1.2.0.
    6. Verify `src/gsd-models.ts:33` reference: check whether `node_modules/@opengsd/gsd-core/sdk/shared/model-catalog.json` still exists. If it does NOT exist, flag this as a deviation requiring user intervention before continuing (per CONTEXT.md Claude's Discretion — impact.md catalogued `sdk/dist/query` removal but did NOT catalogue `sdk/shared/`). Do NOT silently proceed with a broken catalog path.
  </action>
  <verify>
    <automated>
      node -e "require.resolve('@opengsd/gsd-core/package.json')" && \
      node -e "require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')" && \
      grep -c '@opengsd/gsd-core' src/official.ts package.json
    </automated>
  </verify>
  <done>Upstream package constant and dependency point to `@opengsd/gsd-core@1.2.0`; npm install succeeds; CLI bridge path resolves</done>
  <acceptance_criteria>
    - `grep 'OFFICIAL_PACKAGE_NAME' src/official.ts` contains `"@opengsd/gsd-core"`
    - `grep '@opengsd/gsd-core' package.json` shows version `"1.2.0"`
    - `node -e "require.resolve('@opengsd/gsd-core/package.json')"` exits 0
    - `node -e "require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')"` exits 0
    - If `node -e "require.resolve('@opengsd/gsd-core/sdk/shared/model-catalog.json')"` fails, execution halts and reports the deviation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures, inline comments, and path-rewrite references</name>
  <files>tests/fixtures.ts, tests/agent-sync.test.ts, tests/agent-transform.test.ts, tests/cli.test.ts, tests/doctor.test.ts, tests/e2e/doctor-smoke.test.ts, tests/e2e/generate-smoke.test.ts, tests/gsd-models-*.test.ts, tests/official-resolver.test.ts, tests/prompt-transform.test.ts, tests/rewrite-workflow-paths.test.ts, src/rewrite-workflow-paths.ts, src/gsd-models.ts, src/prompt-transform.ts</files>
  <read_first>
    - tests/fixtures.ts:16 (default packageName in createOfficialFixture)
    - tests/official-resolver.test.ts:13,43 (package name assertions)
    - tests/doctor.test.ts:20 (official package message assertion)
    - tests/cli.test.ts:91 (official package message assertion)
    - tests/e2e/doctor-smoke.test.ts:50 (official package message assertion)
    - tests/e2e/generate-smoke.test.ts:126 (node_modules path assertion)
    - tests/prompt-transform.test.ts:375 (PKG constant)
    - tests/rewrite-workflow-paths.test.ts:76,84,92 (path strings)
    - tests/agent-transform.test.ts:100 (path string)
    - tests/agent-sync.test.ts:24 (path string)
    - src/rewrite-workflow-paths.ts:7,25-30,56 (comments referencing old package name)
    - src/gsd-models.ts:33 (comment referencing `sdk/shared/model-catalog.json` with old package name)
    - src/prompt-transform.ts:~710 (comment referencing old package name)
  </read_first>
  <action>
    Perform a global find-and-replace of `@opengsd/get-shit-done-redux` → `@opengsd/gsd-core` across the listed test and source files. Specific locations to update:

    - tests/fixtures.ts: Update default `packageName` parameter default value
    - tests/official-resolver.test.ts: Update expected `packageName` in assertions
    - tests/doctor.test.ts: Update expected `official package:` message assertion
    - tests/cli.test.ts: Update expected `official package:` message assertion
    - tests/e2e/doctor-smoke.test.ts: Update expected official package message
    - tests/e2e/generate-smoke.test.ts: Update `node_modules/@opengsd/get-shit-done-redux` assertion to new name
    - tests/prompt-transform.test.ts: Update `const PKG = "@opengsd/get-shit-done-redux"` and ALL inline test input strings and expectation strings that contain the old package name
    - tests/rewrite-workflow-paths.test.ts: Update path strings in test inputs and expectations
    - tests/agent-transform.test.ts: Update path string in test assertion
    - tests/agent-sync.test.ts: Update path string in test assertion
    - tests/gsd-models-*.test.ts: Update any hardcoded package name references
    - src/rewrite-workflow-paths.ts: Update comments at lines 7, 25-30, 56 that mention the old package name
    - src/gsd-models.ts: Update comment at line 33 that references `@opengsd/get-shit-done-redux/sdk/shared/model-catalog.json`
    - src/prompt-transform.ts: Update comment around line 710 that references the old package name

    Do NOT touch `node_modules/@opengsd/get-shit-done-redux` paths inside `.planning/` history files or CHANGELOG.
  </action>
  <verify>
    <automated>grep -rn "@opengsd/get-shit-done-redux" src/ tests/ | wc -l</automated>
  </verify>
  <done>Zero `@opengsd/get-shit-done-redux` strings remain in src/ and tests/</done>
  <acceptance_criteria>
    - `grep -rn "@opengsd/get-shit-done-redux" src/ tests/` returns empty
    - `npm run check` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
N/A — pure code migration; no new attack surface introduced. Existing surfaces (postinstall, Pi extension hooks) preserve their CLAUDE.md hard rules (never fail / never throw).
</threat_model>

<verification>
- `grep -rn "@opengsd/get-shit-done-redux" src/ tests/` returns empty
- `node -e "require.resolve('@opengsd/gsd-core/package.json')"` succeeds
- `npm run check` green
</verification>

<success_criteria>
- `OFFICIAL_PACKAGE_NAME` constant reads `"@opengsd/gsd-core"`
- package.json dependency is `"@opengsd/gsd-core": "1.2.0"` with old entry removed
- npm install succeeds and module resolution works
- No `@opengsd/get-shit-done-redux` strings remain in src/ or tests/
- `npm run check` green at commit tip
- Commit message ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
</success_criteria>

<output>
Create `.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-02-SUMMARY.md` when done
</output>
