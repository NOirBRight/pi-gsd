---
phase: 08-upstream-1-2-0-upgrade-package-rename
plan: 04
type: execute
wave: 4
depends_on:
  - 08-02
files_modified:
  - src/prompt-transform.ts
  - src/generator.ts
  - tests/prompt-transform.test.ts
  - src/doctor.ts
  - README.md
  - docs/PUBLISHING.md
  - generated/workflows/
  - generated/prompts/
  - generated/agents/
autonomous: true
requirements:
  - UPSTREAM-02
  - UPSTREAM-03

must_haves:
  truths:
    - `transformGsdRunLauncher` exists as a pure `(text: string) => string` function in `src/prompt-transform.ts` with zero fs/path/os imports
    - `applyPromptTransforms` in `src/generator.ts` calls `transformGsdRunLauncher` as the first pipeline step (after external `rewriteWorkflowPaths`, before `normalizeGsdSlashReferences`)
    - `grep "require.resolve('@opengsd/gsd-core" generated/workflows/workflows/discuss-phase.md` succeeds (augmented launcher present in regenerated workflows)
    - New pure-function tests pass: (1) launcher line → augmented block; (2) idempotency; (3) false-positive immunity
    - `dist/cli.js doctor` output contains the `GSD_AUDIT=1` tip
    - `npm run check` is green at commit tip
    - Commit message body ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
  artifacts:
    - path: src/prompt-transform.ts
      provides: "gsd_run launcher pure transform"
      exports: ["transformGsdRunLauncher"]
    - path: tests/prompt-transform.test.ts
      provides: "Launcher transform test coverage"
      contains: "transformGsdRunLauncher"
    - path: generated/workflows/workflows/discuss-phase.md
      provides: "Regenerated workflow with augmented launcher"
      contains: "require.resolve('@opengsd/gsd-core"
  key_links:
    - from: src/prompt-transform.ts
      to: src/generator.ts
      via: "applyPromptTransforms pipeline insertion"
    - from: src/generator.ts
      to: generated/workflows/
      via: "npm run generate produces augmented launcher block"
---

<objective>
Add a new pure string transform `transformGsdRunLauncher` that prepends a `require.resolve`-based `node_modules/` fallback to upstream 1.2.0's inline 4-fallback launcher block. Wire it into the generator pipeline, add tests, surface the `DispatchLogger` opt-in via a `doctor` tip, regenerate all artifacts against 1.2.0, and record release notes.

Purpose: Fix the Pi runtime gap where upstream's 4-fallback launcher never finds `gsd-tools.cjs` inside `node_modules/` (D-33, D-34, D-35). Provide observability guidance without auto-enabling audit writes (D-37).
Output: Working launcher augmentation, regenerated artifacts, `npm run check` green.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-CONTEXT.md
@.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md
@src/prompt-transform.ts
@src/generator.ts
@src/doctor.ts
@tests/prompt-transform.test.ts
@README.md
@docs/PUBLISHING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement transformGsdRunLauncher pure function and pipeline insertion</name>
  <files>src/prompt-transform.ts, src/generator.ts</files>
  <read_first>
    - src/generator.ts:applyPromptTransforms (current pipeline order)
    - src/prompt-transform.ts (area near existing transform exports for placement)
    - upstream-1.2.0-impact.md §gsd_run Launcher Convention (canonical 4-fallback block shape)
  </read_first>
  <action>
    1. In `src/prompt-transform.ts`, add a new exported pure function `transformGsdRunLauncher(text: string): string` near the other transform exports. It MUST NOT import `fs`/`path`/`os` (D-35 pure transform rule). The function:
       - Detects the launcher anchor: the literal substring `_GSD_SHIM_NAME="gsd-tools.cjs"` inside a Bash variable-assignment line
       - When found, prepends a fallback-0 block using `require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')` — the exact Bash shape per D-34:
         ```
         _GSD_SHIM_NAME="gsd-tools.cjs"; GSD_TOOLS="$(node -e "console.log(require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs'))" 2>/dev/null)"; if [ -n "$GSD_TOOLS" ] && [ -f "$GSD_TOOLS" ]; then gsd_run() { node "$GSD_TOOLS" "$@"; }; else <upstream 4-fallback preserved>; fi
         ```
       - The upstream 4-fallback block (everything from the original `_GSD_SHIM_NAME="gsd-tools.cjs"` through the original `fi` or fallback chain end) is preserved verbatim inside the `else` branch
       - Idempotent: running the transform on already-transformed text must return the same text (detect the augmented block and return unchanged)
    2. In `src/generator.ts`, insert `transformGsdRunLauncher` into `applyPromptTransforms` as the **first** step in the pipeline, immediately after the `body` parameter enters the function and before `normalizeGsdSlashReferences`. This satisfies the documented placement: after `rewriteWorkflowPaths` (which runs before `applyPromptTransforms` in both `generatePrompts` and `generateWorkflows`), before the existing transform family.
    3. Add the `transformGsdRunLauncher` import to `src/generator.ts`'s import from `./prompt-transform.js`.
  </action>
  <verify>
    <automated>
      npm run typecheck
    </automated>
  </verify>
  <done>New pure transform compiles and is wired into the pipeline</done>
  <acceptance_criteria>
    - `grep 'export function transformGsdRunLauncher' src/prompt-transform.ts` succeeds
    - `grep 'transformGsdRunLauncher' src/generator.ts` shows it inside `applyPromptTransforms`
    - `npm run typecheck` exits 0
    - `grep -E 'import.*fs|import.*path|import.*os' src/prompt-transform.ts` does not match inside the `transformGsdRunLauncher` region (the whole file already has no such imports; confirm still true)
    - `grep -n "require.resolve('@opengsd/gsd-core" src/prompt-transform.ts` returns at least 1 match (verifies the package name is a hardcoded literal in the transform output, not routed through OFFICIAL_PACKAGE_NAME)
    - `grep -n "OFFICIAL_PACKAGE_NAME" src/prompt-transform.ts` — if any matches exist, verify they are NOT inside the `transformGsdRunLauncher` function body (use line-range check or function boundary check)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add pure-function tests and regenerate 1.2.0 artifacts</name>
  <files>tests/prompt-transform.test.ts, generated/workflows/, generated/prompts/, generated/agents/</files>
  <read_first>
    - tests/prompt-transform.test.ts (existing pure-transform test style, e.g. `describe("transformSkillDispatchForPi", ...)`)
    - src/generator.ts (npm run generate command — `node dist/cli.js generate`)
  </read_first>
  <action>
    1. In `tests/prompt-transform.test.ts`, add a new `describe("transformGsdRunLauncher", () => { ... })` block with three test cases:
       - **(1) Launcher line → augmented block:** Input contains `_GSD_SHIM_NAME="gsd-tools.cjs"` followed by the upstream 4-fallback pattern. Expect output to contain the `require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')` fallback-0 and the preserved upstream block inside `else ... fi`.
       - **(2) Idempotency:** Run `transformGsdRunLauncher` on already-augmented output from test (1). Expect the second run to return exactly the same string as the first.
       - **(3) Negative case — false-positive immunity:** Input contains the substring `gsd-tools.cjs` but NOT the launcher pattern (e.g., a prose sentence "The file gsd-tools.cjs is the CLI bridge"). Expect output to be unchanged.
    2. Run `npm test -- --filter=prompt-transform` (or `npx vitest run tests/prompt-transform.test.ts`) to confirm the new tests pass.
    3. Run `npm run build && npm run generate` to regenerate `generated/prompts/`, `generated/agents/`, and `generated/workflows/` against the installed `@opengsd/gsd-core@1.2.0`.
    4. Verify the regenerated workflows contain the augmented launcher by grepping `generated/workflows/workflows/discuss-phase.md` for the `require.resolve` fallback string.
  </action>
  <verify>
    <automated>
      npx vitest run tests/prompt-transform.test.ts -t "transformGsdRunLauncher" && \
      grep "require.resolve('@opengsd/gsd-core" generated/workflows/workflows/discuss-phase.md
    </automated>
  </verify>
  <done>All three launcher transform tests pass; regenerated workflows contain the augmented launcher block</done>
  <acceptance_criteria>
    - `npx vitest run tests/prompt-transform.test.ts -t "transformGsdRunLauncher"` exits 0 with 3 passing tests
    - `grep "require.resolve('@opengsd/gsd-core" generated/workflows/workflows/discuss-phase.md` succeeds
    - `npm run check` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add doctor GSD_AUDIT tip, README note, and release notes</name>
  <files>src/doctor.ts, README.md, docs/PUBLISHING.md</files>
  <read_first>
    - src/doctor.ts (runDoctor messages array construction — append a tip near the end before return)
    - README.md (find a natural location near the Doctor or Update sections)
    - docs/PUBLISHING.md (look for existing changelog/release-notes section)
  </read_first>
  <action>
    1. In `src/doctor.ts`, append a tip message to the `messages` array inside `runDoctor` (after the existing checks, before the `return { ok, messages }`):
       - Message text: `tip: set GSD_AUDIT=1 to enable dispatch trace at .planning/.gsd-trace.jsonl`
    2. In `README.md`, add a one-paragraph note near the Doctor section explaining:
       - "Set environment variable `GSD_AUDIT=1` before running GSD workflows to enable structured dispatch tracing. Traces are written to `.planning/.gsd-trace.jsonl` in JSON Lines format. This is opt-in and disabled by default."
    3. In `docs/PUBLISHING.md`, append a release-notes-style entry documenting the Phase 8 changes:
       - Project rename: `pi-gsd-redux` → `pi-gsd-core`
       - Upstream migration: `@opengsd/get-shit-done-redux@1.1.0` → `@opengsd/gsd-core@1.2.0`
       - Retired `gsd_query` Pi tool and `$GSD_SDK` transform (SDK bridge removed)
       - Added `transformGsdRunLauncher` for Pi runtime `node_modules/` path resolution
    4. Run `npm run check` to confirm the full gate is green.
  </action>
  <verify>
    <automated>
      node dist/cli.js doctor --prompts generated/prompts --agents --cwd . | grep -c "GSD_AUDIT=1"
      # Expected: >=1
    </automated>
  </verify>
  <done>Doctor output contains the GSD_AUDIT tip; README and release notes are updated; `npm run check` green</done>
  <acceptance_criteria>
    - `node dist/cli.js doctor --prompts generated/prompts --agents --cwd . | grep "GSD_AUDIT=1"` returns at least one match
    - `grep "GSD_AUDIT=1" README.md` succeeds
    - `grep "pi-gsd-core" docs/PUBLISHING.md` succeeds (release notes mention rename)
    - `grep "@opengsd/gsd-core@1.2.0" docs/PUBLISHING.md` succeeds (release notes mention migration)
    - `npm run check` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
N/A — pure code migration; no new attack surface introduced. Existing surfaces (postinstall, Pi extension hooks) preserve their CLAUDE.md hard rules (never fail / never throw).
</threat_model>

<verification>
- `grep "transformGsdRunLauncher" src/prompt-transform.ts` and `src/generator.ts` both succeed
- `npx vitest run tests/prompt-transform.test.ts -t "transformGsdRunLauncher"` passes
- `grep "require.resolve('@opengsd/gsd-core" generated/workflows/workflows/discuss-phase.md` succeeds
- `node dist/cli.js doctor --prompts generated/prompts --agents --cwd . | grep "GSD_AUDIT=1"` succeeds
- `npm run check` green
</verification>

<success_criteria>
- `transformGsdRunLauncher` is a pure function (no fs/path/os imports) in `src/prompt-transform.ts`
- `applyPromptTransforms` calls it as the first pipeline step
- Three pure-function tests pass: augmentation, idempotency, false-positive immunity
- `generated/workflows/` contains the augmented launcher block
- `doctor` output surfaces the `GSD_AUDIT=1` tip
- README documents the opt-in audit behavior
- Release notes in `docs/PUBLISHING.md` cover rename + migration + bridge retirement + launcher transform
- `npm run check` green at commit tip
- Commit message ends with `Phase: 08-upstream-1-2-0-upgrade-package-rename`
</success_criteria>

<output>
Create `.planning/phases/08-upstream-1-2-0-upgrade-package-rename/08-04-SUMMARY.md` when done
</output>
