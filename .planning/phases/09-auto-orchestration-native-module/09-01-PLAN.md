---
phase: 09-auto-orchestration-native-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/types.ts
  - src/orchestrator/settings.ts
  - src/orchestrator/state-machine.ts
  - src/orchestrator/gates.ts
  - src/orchestrator/reconciliation.ts
  - src/orchestrator/index.ts
  - tests/orchestrator.test.ts
  - tests/orchestrator-settings.test.ts
autonomous: true
requirements: [ORCH-01, ORCH-02]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Native orchestrator builds workflow-step Units from workflow settings instead of hardcoded prompt reminders."
    - "D-03/D-04: Ambiguous settings/phase conflicts pause with a user-question resume hint rather than silently choosing."
    - "D-14: State Reconciliation, Tool Contract, and Worktree Safety exist as ordered seams without implementing deferred Phase 10/11/12 behavior."
  artifacts:
    - path: "src/orchestrator/types.ts"
      provides: "Unit, settings, gate, snapshot, event, status, and service API types"
      exports: ["OrchestrationUnit", "ResolvedWorkflowSettings", "GateResult", "OrchestratorStatus", "AutoOrchestrator"]
    - path: "src/orchestrator/settings.ts"
      provides: "settings normalization and Unit queue input"
      exports: ["resolveWorkflowSettings", "buildUnitQueue"]
    - path: "src/orchestrator/state-machine.ts"
      provides: "native Unit transition logic"
      exports: ["startOrchestration", "advanceOrchestration", "resumeOrchestration", "stopOrchestration"]
    - path: "src/orchestrator/gates.ts"
      provides: "artifact/code gate and future-module seam ordering"
      exports: ["runPreDispatchGates", "runPostDispatchGate"]
    - path: "src/orchestrator/index.ts"
      provides: "public orchestrator facade"
      exports: ["createAutoOrchestrator", "start", "advance", "resume", "stop", "getStatus"]
  key_links:
    - from: "src/orchestrator/settings.ts"
      to: "generated/workflows/references/planning-config.md"
      via: "workflow.* defaults encoded in tests"
      pattern: "workflow\\.(auto_advance|research|plan_check|verifier|ui_phase|ui_review|code_review|node_repair|node_repair_budget)"
    - from: "src/orchestrator/state-machine.ts"
      to: "src/orchestrator/gates.ts"
      via: "advance calls gates in ORCH-02 order"
      pattern: "reconcileBeforeDispatch[\\s\\S]*decideDispatch[\\s\\S]*validateToolContract[\\s\\S]*prepareUnitRoot[\\s\\S]*persistRuntimeState"
---

<objective>
Build the native Phase 9 orchestration kernel: typed workflow-step Units, settings-driven queue construction, ordered invariant gate seams, and the public service facade required by ORCH-01/ORCH-02.

Purpose: Replace prompt-compliance loop ownership with deterministic TypeScript state transitions while preserving `/gsd-settings` semantics and leaving Phase 10/11/12 deep modules as seams.
Output: `src/orchestrator/` kernel files and unit tests for settings, queue construction, gate order, pause behavior, and facade methods.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-orchestration-native-module/09-CONTEXT.md
@.planning/phases/09-auto-orchestration-native-module/09-RESEARCH.md
@.planning/phases/09-auto-orchestration-native-module/09-VALIDATION.md
@.planning/phases/09-auto-orchestration-native-module/09-PATTERNS.md
@generated/workflows/workflows/settings.md
@generated/workflows/references/planning-config.md
@generated/workflows/workflows/autonomous.md
@generated/workflows/references/checkpoints.md
@generated/workflows/references/artifact-types.md
@src/doctor.ts
@src/official.ts
@src/pi-subagents.ts
@tests/agent-sync.test.ts
@tests/cli.test.ts
</context>

## Artifacts this phase produces

- Decorators: none.
- Classes: `OrchestratorSettingsError`.
- Types/interfaces: `UnitType`, `OrchestrationUnit`, `UnitStatus`, `WorkflowSettingSource`, `ResolvedWorkflowSettings`, `QueueBuildInput`, `QueueBuildResult`, `GateName`, `GateResult`, `GateAdapter`, `DispatchAdapter`, `JournalAdapter`, `StateDigestAdapter`, `OrchestrationEvent`, `OrchestrationSnapshot`, `OrchestratorStatus`, `OrchestratorSessionContext`, `OrchestratorResult`, `AdvanceResult`, `StopReason`, `AutoOrchestrator`, `ReconcileBeforeDispatchResult`.
- Functions: `resolveWorkflowSettings`, `buildUnitQueue`, `startOrchestration`, `advanceOrchestration`, `resumeOrchestration`, `stopOrchestration`, `getSnapshotStatus`, `runPreDispatchGates`, `runPostDispatchGate`, `reconcileBeforeDispatch`, `createAutoOrchestrator`, `start`, `advance`, `resume`, `stop`, `getStatus`, `createJournalAdapter`, `readJournal`, `writeJournalSnapshot`, `appendJournalEvent`, `redactJournalEvent`, `writeStateDigestPointer`, `createDispatchAdapter`, `dispatchUnit`, `runOrchestratorCli`.
- CLI flags/commands: `orchestrate --auto`, `orchestrate --chain`, `orchestrate --resume`, `orchestrate --status`, `orchestrate --stop <reason>`, `--phase <phase>`, `--cwd <path>`.
- Struct/dataclass fields: `snapshot.version`, `snapshot.phase`, `snapshot.mode`, `snapshot.status`, `snapshot.currentUnit`, `snapshot.remainingUnits`, `snapshot.attempt`, `snapshot.lastEvent`, `snapshot.resumeHint`, `event.type`, `event.ts`, `event.phase`, `event.unitId`, `event.status`, `event.attempt`, `event.reason`, `event.resumeHint`, `event.evidence`, `settings.workflow.auto_advance`, `settings.workflow.research`, `settings.workflow.plan_check`, `settings.workflow.verifier`, `settings.workflow.ui_phase`, `settings.workflow.ui_review`, `settings.workflow.code_review`, `settings.workflow.node_repair`, `settings.workflow.node_repair_budget`, `settings.workflow.worktrees`, `settings.workflow._auto_chain_active`.
- New file paths: `src/orchestrator/index.ts`, `src/orchestrator/types.ts`, `src/orchestrator/state-machine.ts`, `src/orchestrator/settings.ts`, `src/orchestrator/journal.ts`, `src/orchestrator/gates.ts`, `src/orchestrator/dispatch.ts`, `src/orchestrator/state-digest.ts`, `src/orchestrator/reconciliation.ts`, `tests/orchestrator.test.ts`, `tests/orchestrator-settings.test.ts`, `tests/orchestrator-journal.test.ts`, `tests/e2e/orchestrator-chain.test.ts`, `.planning/orchestration-state.json`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define orchestrator contracts and settings-driven Unit queue</name>
  <files>src/orchestrator/types.ts, src/orchestrator/settings.ts, tests/orchestrator-settings.test.ts</files>
  <read_first>
    - src/orchestrator/types.ts if it already exists; otherwise read src/doctor.ts for exported type/result patterns.
    - src/orchestrator/settings.ts if it already exists; otherwise read src/official.ts for resolver/error patterns.
    - tests/orchestrator-settings.test.ts if it already exists; otherwise read tests/cli.test.ts for fixture and assertion style.
    - generated/workflows/workflows/settings.md.
    - generated/workflows/references/planning-config.md.
    - generated/workflows/workflows/autonomous.md.
    - .planning/phases/09-auto-orchestration-native-module/09-CONTEXT.md, decisions D-01 through D-05.
  </read_first>
  <behavior>
    - Test 1: With default workflow settings, `buildUnitQueue({ mode: "chain", phase: "09" })` returns workflow-step Units, not tool-turn Units, per D-01.
    - Test 2: `workflow.research`, `workflow.plan_check`, `workflow.verifier`, `workflow.ui_phase`, `workflow.ui_review`, `workflow.code_review`, `workflow.auto_advance`, `workflow.worktrees`, `workflow.node_repair`, and `workflow.node_repair_budget` are normalized from `.planning/config.json` defaults per D-02/D-03.
    - Test 3: A conflict between a disabled setting and a required phase signal returns a `pause_for_user` queue decision with a resume hint, per D-03/D-04.
  </behavior>
  <action>Create `src/orchestrator/types.ts` with discriminated unions for Unit types `discuss`, `research`, `plan`, `plan-check`, `execute`, `code-review`, `verify`, `ui-review`, `closeout`, `settings-gate`, and `pause-for-user`; define `ResolvedWorkflowSettings` with concrete `workflow.*` keys from `planning-config.md`, including `_auto_chain_active`, `auto_advance`, `research`, `plan_check`, `verifier`, `ui_phase`, `ui_review`, `code_review`, `skip_discuss`, `worktrees`, `node_repair`, and `node_repair_budget`. Create `src/orchestrator/settings.ts` with `resolveWorkflowSettings({ cwd, configPath?, defaults? })` and `buildUnitQueue(input)`. Apply precedence per D-04: explicit invocation mode starts orchestration; persistent `workflow.*` toggles decide Unit inclusion; phase/artifact signals only activate conditional candidates when settings allow them; ambiguity returns a user-question pause Unit rather than silently choosing. Do not add dependencies and do not implement Phase 10 drift repair, Phase 11 worktree validation, or Phase 12 full tool contract.</action>
  <verify>
    <automated>npx vitest run tests/orchestrator-settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/orchestrator/types.ts` exports `OrchestrationUnit`, `ResolvedWorkflowSettings`, `GateResult`, `OrchestratorStatus`, and `AutoOrchestrator`.
    - Source assertion: `src/orchestrator/settings.ts` contains `buildUnitQueue` and references `workflow.node_repair_budget` and `workflow._auto_chain_active`.
    - Behavior assertion: tests cover at least one enabled optional Unit and one disabled optional Unit.
    - Behavior assertion: ambiguous settings return `pause-for-user` with a non-empty `resumeHint` string.
  </acceptance_criteria>
  <done>Settings-derived Unit queues satisfy D-01 through D-05 and ORCH-01 without hardcoded Plan → Execute → Verify-only assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure state transitions and ordered gate seams</name>
  <files>src/orchestrator/state-machine.ts, src/orchestrator/gates.ts, src/orchestrator/reconciliation.ts, tests/orchestrator.test.ts</files>
  <read_first>
    - src/orchestrator/state-machine.ts if it already exists; otherwise read src/prompt-transform.ts for pure function/idempotent result style.
    - src/orchestrator/gates.ts if it already exists; otherwise read src/doctor.ts for never-throw check result style.
    - src/orchestrator/reconciliation.ts if it already exists; otherwise read src/doctor.ts and .planning/phases/09-auto-orchestration-native-module/09-RESEARCH.md Architecture Patterns.
    - tests/orchestrator.test.ts if it already exists; otherwise read tests/agent-sync.test.ts for temp fixture style.
    - .planning/REQUIREMENTS.md ORCH-02.
    - .planning/phases/09-auto-orchestration-native-module/09-CONTEXT.md decisions D-05, D-11, D-13, D-14.
  </read_first>
  <behavior>
    - Test 1: `advanceOrchestration` calls gates in exact ORCH-02 order: State Reconciliation seam, dispatch decision, Tool Contract seam, Worktree Safety seam, runtime persistence.
    - Test 2: A retryable gate failure uses `workflow.node_repair` and increments attempt until `workflow.node_repair_budget` is exhausted, then pauses with typed reason and resume hint per D-05.
    - Test 3: Artifact gate failure after dispatch prevents advancing to the next Unit and records gate-fail evidence per D-13.
  </behavior>
  <action>Create pure transition functions in `src/orchestrator/state-machine.ts`: `startOrchestration`, `advanceOrchestration`, `resumeOrchestration`, `stopOrchestration`, and `getSnapshotStatus`. Create `src/orchestrator/gates.ts` with `runPreDispatchGates` and `runPostDispatchGate`; pre-dispatch order must be `reconcileBeforeDispatch`, `decideDispatch`, `validateToolContract`, `prepareUnitRoot`, `persistRuntimeState` per ORCH-02/D-11. Create `src/orchestrator/reconciliation.ts` with the Phase 10 seam `reconcileBeforeDispatch` that performs only minimal Phase 9 checks and returns structured pass/deferred/blocker results per D-14. Implement artifact gates for Plan (`*-PLAN.md` exists), Execute (`*-SUMMARY.md` exists for completed plan), Verify (`*-VERIFICATION.md` or configured verifier skip), and Closeout (phase status evidence), with missing files converted to `GateResult` failures, not thrown exceptions. Do not implement the full Phase 11 recovery taxonomy; pause reasons should be narrow Phase 9 strings such as `gate-failed`, `ambiguous-dispatch`, `retry-budget-exhausted`, and `dispatch-failed`.</action>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/orchestrator/gates.ts` contains the ordered seam names `reconcileBeforeDispatch`, `decideDispatch`, `validateToolContract`, `prepareUnitRoot`, and `persistRuntimeState` in that order.
    - Source assertion: no `throw new Error` is used for normal gate misses in `src/orchestrator/gates.ts`; gate misses return `{ ok: false, reason, retryable, resumeHint }`.
    - Behavior assertion: a retryable failure with `node_repair_budget: 2` produces exactly two retry attempts before pause.
    - Behavior assertion: a successful Unit transition moves the completed Unit out of `currentUnit` and records the next Unit from `remainingUnits`.
  </acceptance_criteria>
  <done>Native state-machine behavior satisfies ORCH-02, D-05, D-11, D-13, and D-14 with tested gate ordering and pause/retry outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose orchestrator facade with injectable dependencies</name>
  <files>src/orchestrator/index.ts, tests/orchestrator.test.ts</files>
  <read_first>
    - src/orchestrator/index.ts if it already exists; otherwise read src/doctor.ts for service facade result patterns.
    - src/orchestrator/types.ts.
    - src/orchestrator/settings.ts.
    - src/orchestrator/state-machine.ts.
    - src/orchestrator/gates.ts.
    - tests/orchestrator.test.ts.
    - .planning/ROADMAP.md Phase 9 success criteria.
    - .planning/phases/09-auto-orchestration-native-module/09-CONTEXT.md decisions D-11, D-12, D-18.
  </read_first>
  <behavior>
    - Test 1: `createAutoOrchestrator(deps).start(sessionContext)` returns `{ ok: true }` and a status containing `currentUnit`, `remainingUnits`, `attempt`, `lastEvent`, and `resumeHint` per D-18.
    - Test 2: exported facade methods `start`, `advance`, `resume`, `stop`, and `getStatus` exist and delegate to one orchestrator instance without printing.
    - Test 3: injected dispatch adapter receives workflow-step Unit metadata and never receives raw prompt text or tool-turn data per D-01/D-12.
  </behavior>
  <action>Create `src/orchestrator/index.ts` exporting `createAutoOrchestrator` plus the ROADMAP-required facade functions `start(sessionContext)`, `advance()`, `resume()`, `stop(reason)`, and `getStatus()`. Use dependency injection for settings resolver, dispatch adapter, journal adapter, state digest adapter, gates, clock, and filesystem checks. Service functions must return structured records with `ok`, `messages`, `status`, and optional `written`; they must not print to stdout/stderr. Ensure `getStatus()` exposes `currentUnit`, `remainingUnits`, `attempt`, `lastEvent`, and `resumeHint` exactly per D-18. Keep Pi subagent dispatch behind the adapter interface for Plan/Execute/Verify/Closeout per D-12; do not call slash prompts as self-orchestrating loops.</action>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/orchestrator/index.ts` exports `createAutoOrchestrator`, `start`, `advance`, `resume`, `stop`, and `getStatus`.
    - Behavior assertion: tests prove the service facade does not write to `console.log`, `process.stdout`, or `process.stderr`.
    - Behavior assertion: `getStatus().remainingUnits` changes after a successful `advance()`.
    - Behavior assertion: dispatch test doubles receive Unit types such as `plan`, `execute`, `verify`, or `closeout`, not individual model/tool-turn identifiers.
  </acceptance_criteria>
  <done>Public Phase 9 orchestrator API exists and is testable without real Pi subagent execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/Pi invocation → orchestrator settings | User-controlled flags and `.planning/config.json` values influence Unit queue and dispatch. |
| Orchestrator → filesystem gates | Gate checks read `.planning/` artifacts and must not treat missing/tampered files as success. |
| Orchestrator → future module seams | Phase 10/11/12 seams can block dispatch; failures must be structured and auditable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `buildUnitQueue` settings inputs | mitigate | Validate known `workflow.*` keys and pause on ambiguous setting/phase conflicts per D-03/D-04 instead of silently dispatching. |
| T-09-02 | Elevation of Privilege | dispatch decision gate | mitigate | Unit types are a closed union; unknown Unit types produce `GateResult { ok: false }` before dispatch. |
| T-09-03 | Repudiation | gate order | mitigate | Tests assert ORCH-02 order and event evidence for gate pass/fail. |
| T-09-04 | Information Disclosure | status/result messages | mitigate | Status contains IDs, paths, event kind, attempts, and short reasons only; no raw prompts, secrets, tokens, env dumps, or unbounded user text per D-17. |
| T-09-SC | Tampering | npm installs | accept | Phase 9 Plan 01 adds no package-manager install tasks; Package Legitimacy Audit states no new packages. |
</threat_model>

<verification>
- `npx vitest run tests/orchestrator-settings.test.ts tests/orchestrator.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- ORCH-01: Native Unit queue and state-machine API exist without prompt-loop dependency.
- ORCH-02: Gate order is implemented as test-covered seams.
- All tasks cite and implement D-01 through D-05, D-11 through D-14, and D-18 where applicable.
</success_criteria>

<output>
Create `.planning/phases/09-auto-orchestration-native-module/09-01-SUMMARY.md` when done.
</output>
