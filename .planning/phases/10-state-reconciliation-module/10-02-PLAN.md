---
phase: 10-state-reconciliation-module
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/state-reconciliation/catalog.ts
  - src/state-reconciliation/roadmap.ts
  - src/state-reconciliation/state.ts
  - src/state-reconciliation/journal.ts
  - src/state-reconciliation/drift/sketch-flag.ts
  - src/state-reconciliation/drift/completion-timestamp.ts
  - src/state-reconciliation/drift/roadmap-divergence.ts
  - src/state-reconciliation/drift/stale-worker.ts
  - src/state-reconciliation/drift/unregistered-milestone.ts
  - src/state-reconciliation/drift/summary-count-mismatch.ts
  - src/state-reconciliation/drift/noncanonical-plan-like-file.ts
  - tests/state-reconciliation.test.ts
autonomous: true
requirements: [STATE-02]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Missing or ambiguous content artifacts become typed blockers with stable reason codes and evidence paths."
    - "D-03/D-04: Drift classification uses canonical phase artifacts before ROADMAP/STATE and treats noncanonical markdown as evidence only."
    - "D-10: The catalog covers the five ROADMAP-known cases plus `summary-count-mismatch`, `noncanonical-plan-like-file`, and `unknown-drift` fallback."
  artifacts:
    - path: "src/state-reconciliation/catalog.ts"
      provides: "known drift catalog and fallback classification"
      exports: ["classifyDrift", "KNOWN_DRIFT_KINDS"]
    - path: "src/state-reconciliation/roadmap.ts"
      provides: "ROADMAP metadata parser and repair candidates"
      exports: ["readRoadmapState"]
    - path: "src/state-reconciliation/state.ts"
      provides: "STATE metadata parser and repair candidates"
      exports: ["readStateDigest"]
    - path: "src/state-reconciliation/journal.ts"
      provides: "journal evidence reader"
      exports: ["readJournalState"]
  key_links:
    - from: "src/state-reconciliation/catalog.ts"
      to: "src/state-reconciliation/drift/*.ts"
      via: "ordered detector registry"
      pattern: "summary-count-mismatch|noncanonical-plan-like-file|unknown-drift"
---

<objective>
Implement the drift catalog and narrow ROADMAP/STATE/journal metadata readers.

Purpose: Cover all D-10 drift kinds without writing files yet, preserving conservative blocker semantics for ambiguous or content-bearing problems.
Output: Detector registry, seven drift detector modules, metadata parsers, and table-driven catalog tests.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-state-reconciliation-module/10-CONTEXT.md
@.planning/phases/10-state-reconciliation-module/10-RESEARCH.md
@.planning/phases/10-state-reconciliation-module/10-01-SUMMARY.md
@generated/workflows/templates/state.md
@src/state-reconciliation/index.ts
@src/state-reconciliation/types.ts
@src/state-reconciliation/scan.ts
@tests/state-reconciliation.test.ts
</context>

## Artifacts this phase produces

- Types/interfaces: `DriftDetection`, detector result records if needed.
- Functions: `classifyDrift`, `readRoadmapState`, `readStateDigest`, `readJournalState`, and one detector function per drift module.
- New file paths: `src/state-reconciliation/catalog.ts`, `src/state-reconciliation/roadmap.ts`, `src/state-reconciliation/state.ts`, `src/state-reconciliation/journal.ts`, `src/state-reconciliation/drift/*.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse derived ROADMAP, STATE, and journal evidence</name>
  <files>src/state-reconciliation/roadmap.ts, src/state-reconciliation/state.ts, src/state-reconciliation/journal.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md Phase 10 and progress table format.
    - .planning/STATE.md frontmatter, Current Position, and Progress sections.
    - generated/workflows/templates/state.md.
    - src/orchestrator/journal.ts for journal shape and corrupt-journal behavior.
  </read_first>
  <behavior>
    - Test 1: ROADMAP phase rows parse plan counts, status, and timestamp.
    - Test 2: STATE frontmatter/current-position metadata parses without reading unrelated prose as truth.
    - Test 3: Corrupt `orchestration-state.json` returns evidence/blocker data and does not overwrite content.
  </behavior>
  <action>Create read-only parsers for ROADMAP, STATE, and journal metadata. These helpers should never synthesize missing content and should return structured evidence/blocker candidates to the catalog.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "roadmap|state digest|journal"</automated>
  </verify>
  <acceptance_criteria>
    - Parsers are read-only.
    - Corrupt journal data fails closed.
    - ROADMAP/STATE are treated as derived metadata, not source of truth.
  </acceptance_criteria>
  <done>Derived metadata can be compared against canonical artifact snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detector registry and low-risk artifact drift detectors</name>
  <files>src/state-reconciliation/catalog.ts, src/state-reconciliation/drift/summary-count-mismatch.ts, src/state-reconciliation/drift/noncanonical-plan-like-file.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-01, D-04, D-10.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Drift Catalog.
  </read_first>
  <behavior>
    - Test 1: Missing summaries for completed/executed plan evidence produce `summary-count-mismatch` blockers.
    - Test 2: `09-PLAN-CHECK.md` produces `noncanonical-plan-like-file` evidence and never counts as a plan.
    - Test 3: `KNOWN_DRIFT_KINDS` includes all required D-10 kinds.
  </behavior>
  <action>Create `catalog.ts` with an ordered detector registry and implement the two concrete drift kinds from this session: `summary-count-mismatch` and `noncanonical-plan-like-file`.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "summary-count|noncanonical|KNOWN_DRIFT"</automated>
  </verify>
  <acceptance_criteria>
    - Missing summary content is blocked, not synthesized.
    - Noncanonical plan-like files are evidence only.
  </acceptance_criteria>
  <done>The catalog handles the concrete drift that motivated Phase 10 planning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add repairable ROADMAP metadata drift detectors</name>
  <files>src/state-reconciliation/drift/completion-timestamp.ts, src/state-reconciliation/drift/roadmap-divergence.ts, src/state-reconciliation/catalog.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-01, D-02, D-03, D-09, D-10.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Risks And Open Questions.
  </read_first>
  <behavior>
    - Test 1: `roadmap-divergence` and `completion-timestamp-drift` emit repair candidates only when canonical artifacts prove the metadata value.
    - Test 2: The same detectors return blockers when canonical artifacts do not mechanically prove the metadata value.
  </behavior>
  <action>Implement the two repairable metadata detector modules: `completion-timestamp-drift` and `roadmap-divergence`. Wire them into `catalog.ts` with tests that first fail on each missing detector, then pass after the detector emits the expected typed repair candidate or blocker.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "roadmap divergence|completion timestamp"</automated>
  </verify>
  <acceptance_criteria>
    - `completion-timestamp-drift` and `roadmap-divergence` are both covered by red/green tests.
    - Repair candidates are emitted only when canonical artifacts prove the derived value.
    - Detector modules do not write files directly.
  </acceptance_criteria>
  <done>Repairable ROADMAP metadata drift is detected without applying writes or synthesizing content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add blocking/evidence ROADMAP-known detectors and unknown fallback</name>
  <files>src/state-reconciliation/drift/sketch-flag.ts, src/state-reconciliation/drift/stale-worker.ts, src/state-reconciliation/drift/unregistered-milestone.ts, src/state-reconciliation/catalog.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-01, D-02, D-09, D-10.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Risks And Open Questions.
  </read_first>
  <behavior>
    - Test 1: `sketch-flag-drift` blocks when the correct sketch metadata is not mechanically provable.
    - Test 2: `stale-worker` blocks with category-level evidence and does not choose retry/pause/self-heal/stop.
    - Test 3: `unregistered-milestone` blocks rather than creating milestone prose.
    - Test 4: Unsupported mismatches become `unknown-drift`.
  </behavior>
  <action>Implement the remaining ROADMAP-known detector modules and the `unknown-drift` fallback. Keep each detector on its own red/green loop in `tests/state-reconciliation.test.ts`: write the failing assertion for one detector, implement only that detector, then move to the next. Do not choose Phase 11 recovery actions or create missing milestone/content prose.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "sketch flag|stale worker|unregistered milestone|unknown drift"</automated>
  </verify>
  <acceptance_criteria>
    - `sketch-flag-drift`, `stale-worker`, `unregistered-milestone`, and `unknown-drift` are all covered by targeted tests.
    - Blocking detectors produce stable reason codes and evidence paths.
    - Detector modules do not write files directly.
  </acceptance_criteria>
  <done>All D-10 detector coverage exists without scope leakage into Phase 11 recovery or content synthesis.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | detector registry | mitigate | Detector outputs are typed drift records, not writes. |
| T-10-04 | Repudiation | blockers | mitigate | Every blocker has stable reason code and evidence paths. |
| T-10-05 | Denial of Service | unsupported drift | mitigate | Unknown drift blocks instead of silently passing. |
</threat_model>

<verification>
- `npx vitest run tests/state-reconciliation.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- STATE-02: Drift catalog covers all D-10 minimum drift kinds.
- No detector performs writes or content synthesis.
</success_criteria>

<output>
Create `.planning/phases/10-state-reconciliation-module/10-02-SUMMARY.md` when done.
</output>
