---
phase: 10-state-reconciliation-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state-reconciliation/types.ts
  - src/state-reconciliation/artifacts.ts
  - src/state-reconciliation/scan.ts
  - src/state-reconciliation/index.ts
  - tests/state-reconciliation.test.ts
autonomous: true
requirements: [STATE-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-03/D-04: Canonical files under `.planning/phases/` are the source of truth; ROADMAP, STATE, and journal data are derived, and noncanonical markdown never counts as canonical plan/summary/verification/review content."
    - "D-02/D-05: The module returns structured reports with stable `reasonCode`, `evidence`, `repairs`, `written`, and `blockers` fields instead of prose-only drift descriptions."
    - "D-06: `reconcileBeforeDispatch(basePath, options?)` defaults to dry-run and records no writes unless apply mode is explicitly requested by later plans."
  artifacts:
    - path: "src/state-reconciliation/types.ts"
      provides: "typed snapshot, evidence, repair, blocker, write, and report contracts"
      exports: ["ReconciliationReport", "ReconciliationBlocker", "ReconciliationRepair", "ReconciliationWrite", "ReconciledStateSnapshot"]
    - path: "src/state-reconciliation/artifacts.ts"
      provides: "canonical artifact-name classifier"
      exports: ["classifyArtifactName"]
    - path: "src/state-reconciliation/scan.ts"
      provides: "read-only `.planning/phases/` scanner"
      exports: ["scanPlanningArtifacts"]
    - path: "src/state-reconciliation/index.ts"
      provides: "initial public reconciliation API"
      exports: ["reconcileBeforeDispatch"]
  key_links:
    - from: "src/state-reconciliation/scan.ts"
      to: ".planning/phases/"
      via: "canonical artifact scanner"
      pattern: "PLAN\\.md|SUMMARY\\.md|VERIFICATION\\.md|REVIEW\\.md|CONTEXT\\.md"
---

<objective>
Create the typed reconciliation contracts and read-only canonical artifact scanner.

Purpose: Establish the native `src/state-reconciliation/` public surface without implementing drift repair yet, so later plans can build on stable report/blocker/evidence types.
Output: Type contracts, artifact classifier, scanner, initial `reconcileBeforeDispatch` report, and focused unit tests.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-state-reconciliation-module/10-CONTEXT.md
@.planning/phases/10-state-reconciliation-module/10-RESEARCH.md
@generated/workflows/references/artifact-types.md
@src/orchestrator/reconciliation.ts
@src/orchestrator/types.ts
@tests/orchestrator.test.ts
</context>

## Artifacts this phase produces

- Types/interfaces: `ReconciliationOptions`, `ReconciliationReport`, `ReconciledStateSnapshot`, `ReconciliationEvidence`, `ReconciliationRepair`, `ReconciliationWrite`, `ReconciliationBlocker`, `ReconciliationReasonCode`, `CanonicalArtifactKind`, `CanonicalPhaseArtifacts`, `PlanningArtifactScan`.
- Functions: `classifyArtifactName`, `scanPlanningArtifacts`, `reconcileBeforeDispatch`.
- New file paths: `src/state-reconciliation/types.ts`, `src/state-reconciliation/artifacts.ts`, `src/state-reconciliation/scan.ts`, `src/state-reconciliation/index.ts`, `tests/state-reconciliation.test.ts`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define report and blocker contracts</name>
  <files>src/state-reconciliation/types.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md decisions D-01 through D-06 and D-10.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Recommended Module Breakdown.
    - src/orchestrator/types.ts for existing `GateResult` style.
  </read_first>
  <behavior>
    - Test 1: A minimal report object has `ok`, `snapshot`, `repairs`, `blockers`, `written`, and `evidence` fields.
    - Test 2: `ReconciliationReasonCode` accepts all D-10 drift names plus `unknown-drift` and `partial-write`.
  </behavior>
  <action>Create `types.ts` with exported report, snapshot, repair, write, evidence, blocker, and reason-code types. Keep fields stable and machine-readable; do not encode blockers as strings only.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "contracts"</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` exports `ReconciliationReport`, `ReconciliationBlocker`, `ReconciliationRepair`, `ReconciliationWrite`, and `ReconciledStateSnapshot`.
    - Reason-code union includes `summary-count-mismatch`, `noncanonical-plan-like-file`, `unknown-drift`, and `partial-write`.
  </acceptance_criteria>
  <done>Stable typed reconciliation contracts exist for later scanner, catalog, repair, and orchestrator work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify canonical artifact names only</name>
  <files>src/state-reconciliation/artifacts.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - generated/workflows/references/artifact-types.md.
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md decisions D-03 and D-04.
  </read_first>
  <behavior>
    - Test 1: `10-01-PLAN.md`, `10-01-SUMMARY.md`, `10-VERIFICATION.md`, `10-REVIEW.md`, and `10-CONTEXT.md` classify as canonical.
    - Test 2: `09-PLAN-CHECK.md` classifies as noncanonical plan-like evidence and does not count as a plan.
  </behavior>
  <action>Create `artifacts.ts` with `classifyArtifactName(filename)` and narrow regex helpers for canonical GSD artifacts. Return typed classification records with `kind`, `phase`, optional `plan`, and evidence reason for noncanonical plan-like names.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "canonical artifact|noncanonical"</automated>
  </verify>
  <acceptance_criteria>
    - Canonical matching covers PLAN, SUMMARY, VERIFICATION, REVIEW, and CONTEXT.
    - `PLAN-CHECK.md` is not counted as a plan.
  </acceptance_criteria>
  <done>Artifact classification enforces D-04 before any drift detection is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build read-only scanner and initial dry-run API</name>
  <files>src/state-reconciliation/scan.ts, src/state-reconciliation/index.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - src/state-reconciliation/types.ts.
    - src/state-reconciliation/artifacts.ts.
    - tests/orchestrator.test.ts for temp project fixture style.
  </read_first>
  <behavior>
    - Test 1: A temp `.planning/phases/` tree produces canonical counts and evidence paths.
    - Test 2: Missing `.planning/` returns a typed blocker, not thrown control flow.
    - Test 3: `reconcileBeforeDispatch(root)` defaults to dry-run and returns `written: []`.
  </behavior>
  <action>Create `scanPlanningArtifacts(basePath)` and the initial `reconcileBeforeDispatch(basePath, options?)`. The scanner is read-only, walks phase directories, records canonical artifacts and noncanonical evidence, and does not inspect ROADMAP/STATE yet. The public API should assemble a structured report with empty repairs and no writes.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "scanner|structured report"</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run report has `written.length === 0`.
    - Missing planning directories produce `ok: false` with a stable `reasonCode`.
    - No source file imports `gsd_query`.
  </acceptance_criteria>
  <done>STATE-01 has a native read-only reconciliation entry point and artifact snapshot foundation.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `classifyArtifactName` | mitigate | Count only canonical GSD names and record noncanonical plan-like files as evidence. |
| T-10-02 | Spoofing | scanner truth source | mitigate | Canonical phase artifacts are the only counted truth source in this plan. |
| T-10-SC | Tampering | npm installs | accept | No package-manager install tasks; use existing Node/Vitest stack. |
</threat_model>

<verification>
- `npx vitest run tests/state-reconciliation.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- STATE-01: `src/state-reconciliation/` exists and exposes a typed native dry-run API.
- D-01 through D-06 and D-10 scanner-facing decisions are represented in types and tests.
</success_criteria>

<output>
Create `.planning/phases/10-state-reconciliation-module/10-01-SUMMARY.md` when done.
</output>
