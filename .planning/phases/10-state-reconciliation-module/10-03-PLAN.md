---
phase: 10-state-reconciliation-module
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/state-reconciliation/repair.ts
  - src/state-reconciliation/index.ts
  - src/state-reconciliation/roadmap.ts
  - src/state-reconciliation/state.ts
  - src/state-reconciliation/journal.ts
  - tests/state-reconciliation.test.ts
autonomous: true
requirements: [STATE-02]
user_setup: []
must_haves:
  truths:
    - "D-01/D-06: Missing or content-bearing artifacts remain blockers; dry-run reports proposed repairs and writes nothing."
    - "D-07: Apply mode plans repairs first, checks preconditions before each write, and reports `partial-write` with already-written paths if application fails."
    - "D-03: Repair targets are derived ROADMAP/STATE/journal metadata only, mechanically computed from canonical artifacts."
  artifacts:
    - path: "src/state-reconciliation/repair.ts"
      provides: "dry-run/apply repair planning and transaction safety"
      exports: ["planRepairs", "applyRepairs"]
    - path: "src/state-reconciliation/index.ts"
      provides: "assembled scan -> catalog -> repair report flow"
      exports: ["reconcileBeforeDispatch"]
  key_links:
    - from: "src/state-reconciliation/repair.ts"
      to: "src/state-reconciliation/roadmap.ts"
      via: "metadata repair plans"
      pattern: "partial-write|apply"
---

<objective>
Implement deterministic metadata repair planning and explicit apply mode.

Purpose: Make repair behavior idempotent and safe: default dry-run, explicit writes only, preconditions before each write, and typed partial-write reporting.
Output: Repair engine, apply-mode integration, metadata writer helpers, and idempotence tests.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-state-reconciliation-module/10-CONTEXT.md
@.planning/phases/10-state-reconciliation-module/10-RESEARCH.md
@.planning/phases/10-state-reconciliation-module/10-01-SUMMARY.md
@.planning/phases/10-state-reconciliation-module/10-02-SUMMARY.md
@src/state-reconciliation/index.ts
@src/state-reconciliation/types.ts
@src/state-reconciliation/catalog.ts
@src/state-reconciliation/roadmap.ts
@src/state-reconciliation/state.ts
@src/state-reconciliation/journal.ts
@tests/state-reconciliation.test.ts
</context>

## Artifacts this phase produces

- Functions: `planRepairs`, `applyRepairs`, metadata writer helpers for ROADMAP/STATE/journal.
- Struct/dataclass fields: `repair.kind`, `repair.path`, `repair.before`, `repair.after`, `write.path`, `write.kind`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plan repairs without writing</name>
  <files>src/state-reconciliation/repair.ts, src/state-reconciliation/index.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-01, D-06.
    - src/state-reconciliation/catalog.ts.
  </read_first>
  <behavior>
    - Test 1: Repairable drift produces `repairs[]` in dry-run and `written: []`.
    - Test 2: Blocking drift produces no repair records.
  </behavior>
  <action>Create `planRepairs(reportOrDetections)` in `repair.ts` and wire `index.ts` so dry-run reports deterministic repair candidates without file mutation.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "dry-run|plan repairs"</automated>
  </verify>
  <acceptance_criteria>
    - `reconcileBeforeDispatch(basePath)` defaults `apply` to false.
    - Dry-run leaves target files byte-for-byte unchanged.
  </acceptance_criteria>
  <done>Repair plans are visible without side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply deterministic metadata repairs idempotently</name>
  <files>src/state-reconciliation/repair.ts, src/state-reconciliation/roadmap.ts, src/state-reconciliation/state.ts, src/state-reconciliation/journal.ts, src/state-reconciliation/index.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - generated/workflows/templates/state.md.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Fixture And Test Strategy.
  </read_first>
  <behavior>
    - Test 1: `apply: true` writes ROADMAP/STATE/journal metadata repairs and records paths in `written[]`.
    - Test 2: A second apply produces no additional writes.
    - Test 3: Repairs are confined to `.planning/`.
  </behavior>
  <action>Add `applyRepairs` with injectable filesystem operations for tests. Writers may change only existing ROADMAP row fields, STATE frontmatter/progress/current-position metadata, and journal JSON metadata when mechanically derived from canonical artifacts.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "apply|idempotent|written"</automated>
  </verify>
  <acceptance_criteria>
    - First apply writes expected paths.
    - Second apply reports no writes.
    - No repair targets source files.
  </acceptance_criteria>
  <done>Deterministic metadata repair is explicit and idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Report partial-write failures with preserved evidence</name>
  <files>src/state-reconciliation/repair.ts, src/state-reconciliation/index.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-07.
    - tests/orchestrator-journal.test.ts for injected failure patterns.
  </read_first>
  <behavior>
    - Test 1: Simulated write failure after one successful write returns `ok: false`.
    - Test 2: Report contains `reasonCode: "partial-write"` and already-written paths in `written[]`.
  </behavior>
  <action>Run precondition checks before each write and catch apply failures into a typed `partial-write` blocker. Preserve already-written path evidence and do not attempt complex rollback.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "partial-write"</automated>
  </verify>
  <acceptance_criteria>
    - Partial-write tests do not use real destructive paths.
    - `written[]` is accurate after simulated failure.
  </acceptance_criteria>
  <done>Apply failures are recoverable by Phase 11 because written paths and blocker context are preserved.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | `applyRepairs` | mitigate | Dry-run default, explicit apply, precondition checks, `.planning/` confinement. |
| T-10-07 | Repudiation | partial writes | mitigate | Return `partial-write` with `written[]` rather than hiding failure. |
| T-10-SC | Tampering | npm installs | accept | No package-manager install tasks. |
</threat_model>

<verification>
- `npx vitest run tests/state-reconciliation.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- STATE-02: Every repairable drift has dry-run, apply, and second-apply idempotence coverage.
- D-06/D-07 repair semantics are test-proven.
</success_criteria>

<output>
Create `.planning/phases/10-state-reconciliation-module/10-03-SUMMARY.md` when done.
</output>
