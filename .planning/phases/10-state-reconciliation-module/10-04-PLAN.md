---
phase: 10-state-reconciliation-module
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - src/state-reconciliation/errors.ts
  - src/state-reconciliation/index.ts
  - src/orchestrator/reconciliation.ts
  - src/orchestrator/gates.ts
  - src/orchestrator/types.ts
  - tests/state-reconciliation.test.ts
  - tests/orchestrator.test.ts
  - tests/orchestrator-journal.test.ts
  - tests/e2e/orchestrator-chain.test.ts
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03]
user_setup: []
must_haves:
  truths:
    - "D-05/D-08: `ReconciliationFailedError` carries `reasonCode`, `blockers[]`, `repairPlan`, `evidence[]`, and `suggestedNextAction`, and the orchestrator adapter maps that context into a structured gate failure."
    - "D-06/D-09: Pre-dispatch reconciliation defaults to dry-run unless explicit apply is enabled; suggested next actions remain category-level values."
    - "D-01/D-02/D-03/D-04/D-10: E2E gate coverage proves pre-dispatch uses the native catalog before dispatch, honors artifact-first truth, emits typed blockers, ignores noncanonical artifacts for counts, and covers the minimum drift catalog."
  artifacts:
    - path: "src/state-reconciliation/errors.ts"
      provides: "Phase 11 handoff error contract"
      exports: ["ReconciliationFailedError"]
    - path: "src/orchestrator/reconciliation.ts"
      provides: "compatibility adapter from Phase 9 gate seam to native reconciliation module"
      exports: ["reconcileBeforeDispatch"]
    - path: "tests/e2e/orchestrator-chain.test.ts"
      provides: "chain gate coverage for reconciliation before dispatch"
  key_links:
    - from: "src/orchestrator/reconciliation.ts"
      to: "src/state-reconciliation/index.ts"
      via: "adapter calls native reconcileBeforeDispatch(basePath, options)"
      pattern: "state-reconciliation[\\s\\S]*reconcileBeforeDispatch"
---

<objective>
Wire native reconciliation into the orchestrator seam and add Phase 11 handoff evidence.

Purpose: Complete STATE-03 by running reconciliation before every native dispatch without changing Phase 9 gate ownership or implementing Phase 11 recovery classification.
Output: Adapter integration, `ReconciliationFailedError`, journal-safe evidence, e2e blocking coverage, and final verification.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/gsd-core/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-state-reconciliation-module/10-CONTEXT.md
@.planning/phases/10-state-reconciliation-module/10-RESEARCH.md
@.planning/phases/10-state-reconciliation-module/10-01-SUMMARY.md
@.planning/phases/10-state-reconciliation-module/10-02-SUMMARY.md
@.planning/phases/10-state-reconciliation-module/10-03-SUMMARY.md
@src/state-reconciliation/index.ts
@src/state-reconciliation/types.ts
@src/orchestrator/reconciliation.ts
@src/orchestrator/gates.ts
@src/orchestrator/types.ts
@src/orchestrator/journal.ts
@tests/orchestrator.test.ts
@tests/orchestrator-journal.test.ts
@tests/e2e/orchestrator-chain.test.ts
</context>

## Artifacts this phase produces

- Classes: `ReconciliationFailedError`.
- Types/interfaces: `ReconciliationFailureContext`, `ReconciliationSuggestedNextAction`, `ReconciliationGateEvidence`.
- Functions: orchestrator adapter `reconcileBeforeDispatch(snapshot, unit)`, `toReconciliationFailedError`, `toGateFailure`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReconciliationFailedError handoff contract</name>
  <files>src/state-reconciliation/errors.ts, src/state-reconciliation/index.ts, src/state-reconciliation/types.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - .planning/phases/10-state-reconciliation-module/10-CONTEXT.md D-02, D-05, D-08, D-09.
    - src/state-reconciliation/index.ts.
  </read_first>
  <behavior>
    - Test 1: `new ReconciliationFailedError(report)` exposes `reasonCode`, `blockers`, `repairPlan`, `evidence`, and `suggestedNextAction`.
    - Test 2: Suggested next action values are only `manual-review`, `rerun-reconcile`, or `requires-recovery-classification`.
  </behavior>
  <action>Create `errors.ts` and re-export the error/context types from `index.ts`. Do not require parsing `error.message` to retrieve blocker data.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts -t "ReconciliationFailedError|suggested next action"</automated>
  </verify>
  <acceptance_criteria>
    - `ReconciliationFailedError` preserves blocker and repair plan objects.
    - No Phase 11 concrete recovery action is selected.
  </acceptance_criteria>
  <done>Blocked reconciliation has a structured Phase 11 handoff surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Phase 9 minimal seam with native adapter</name>
  <files>src/orchestrator/reconciliation.ts, src/orchestrator/gates.ts, src/orchestrator/types.ts, tests/orchestrator.test.ts</files>
  <read_first>
    - src/orchestrator/reconciliation.ts.
    - src/orchestrator/gates.ts.
    - src/orchestrator/types.ts.
    - tests/orchestrator.test.ts.
  </read_first>
  <behavior>
    - Test 1: Existing gate-order test still sees `reconcileBeforeDispatch` before all other pre-dispatch gates.
    - Test 2: Reconciliation blockers prevent dispatch and return `GateResult` with `gate: "reconcileBeforeDispatch"`.
    - Test 3: Old snapshot status/current-unit ambiguity checks still work.
  </behavior>
  <action>Update the orchestrator adapter to call `src/state-reconciliation/index.ts`, derive `basePath` from `snapshot.cwd ?? process.cwd()`, preserve Phase 9 minimal ambiguity checks, and convert failed reports into non-throwing gate failures with bounded evidence.</action>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts -t "reconcileBeforeDispatch|gate order|dispatch"</automated>
  </verify>
  <acceptance_criteria>
    - Drift classification logic remains in `src/state-reconciliation/`, not `src/orchestrator/gates.ts`.
    - A reconciliation blocker leaves dispatch call count at 0.
  </acceptance_criteria>
  <done>Native reconciliation gates every dispatch through the existing Phase 9 seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add e2e blocking coverage and final verification</name>
  <files>tests/e2e/orchestrator-chain.test.ts, tests/orchestrator.test.ts, tests/orchestrator-journal.test.ts, tests/state-reconciliation.test.ts</files>
  <read_first>
    - tests/e2e/orchestrator-chain.test.ts.
    - tests/orchestrator-journal.test.ts.
    - .planning/phases/10-state-reconciliation-module/10-RESEARCH.md Fixture And Test Strategy.
  </read_first>
  <behavior>
    - Test 1: E2E chain fixture with `summary-count-mismatch` pauses before dispatch with typed reconciliation gate failure.
    - Test 2: E2E chain fixture with `09-PLAN-CHECK.md` proves the file is evidence only and does not increase plan count.
    - Test 3: Journal evidence contains bounded reason codes/paths, not raw markdown body text.
  </behavior>
  <action>Extend e2e and journal tests for reconciliation-specific dispatch blocking and bounded evidence. Finish with targeted tests, typecheck, build, and full `npm run check`. Do not run snapshot/golden update commands.</action>
  <verify>
    <automated>npx vitest run tests/state-reconciliation.test.ts tests/orchestrator.test.ts tests/orchestrator-journal.test.ts tests/e2e/orchestrator-chain.test.ts && npm run typecheck && npm run build && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Blocker e2e fixture does not call dispatch.
    - Passing fixture dispatches after reconciliation passes.
    - `npm run check` exits 0.
  </acceptance_criteria>
  <done>STATE-01, STATE-02, and STATE-03 are verified end-to-end for Phase 10.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Elevation of Privilege | orchestrator adapter | mitigate | Failed reconciliation blocks before dispatch. |
| T-10-09 | Information Disclosure | journal evidence | mitigate | Store bounded reason/path evidence only. |
| T-10-10 | Tampering | apply mode | mitigate | Apply remains explicit; default orchestrator path is dry-run. |
</threat_model>

<verification>
- `npx vitest run tests/state-reconciliation.test.ts tests/orchestrator.test.ts tests/orchestrator-journal.test.ts tests/e2e/orchestrator-chain.test.ts`
- `npm run typecheck`
- `npm run build`
- `npm run check`
</verification>

<success_criteria>
- STATE-01: Orchestration no longer depends on `gsd_query` for `.planning/` state reconciliation.
- STATE-02: Deterministic ROADMAP/STATE/journal metadata drift is detected, repaired only under explicit apply mode, and proven idempotent.
- STATE-03: Auto Orchestration calls native reconciliation before every dispatch and pauses with typed handoff data when blockers exist.
</success_criteria>

<output>
Create `.planning/phases/10-state-reconciliation-module/10-04-SUMMARY.md` when done.
</output>
