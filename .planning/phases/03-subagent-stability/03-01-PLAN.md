---
phase: 03-subagent-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - D:/Workstation/pi-subagents/src/extension/index.ts
  - D:/Workstation/pi-subagents/src/shared/types.ts
  - D:/Workstation/pi-subagents/src/extension/doctor.ts
  - D:/Workstation/pi-subagents/src/runs/background/async-execution.ts
autonomous: true
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "Pi starts without crash on ACL-corrupted temp dirs (ensureAccessibleDir catches EPERM and falls back to pid-scoped path)"
    - "Async subagent results reach consumer modules even when primary temp dir is ACL-corrupted (fallback paths propagate correctly via DIRS container)"
    - "An upstream PR is submitted to nicobailon/pi-subagents with these fixes"
  artifacts:
    - path: "fork:pi-subagents/src/extension/index.ts"
      provides: "ensureAccessibleDir with EPERM catch + fallback + return value"
      contains: "err?.code !== 'EPERM'"
    - path: "fork:pi-subagents/src/shared/types.ts"
      provides: "DIRS mutable container replacing export const RESULTS_DIR/ASYNC_DIR"
      contains: "export const DIRS"
    - path: "fork:pi-subagents/src/runs/background/async-execution.ts"
      provides: "Updated direct imports to use DIRS.results/DIRS.async"
      contains: "DIRS.results"
  key_links:
    - from: "fork:pi-subagents/src/extension/index.ts"
      to: "fork:pi-subagents/src/shared/types.ts"
      via: "DIRS.results = ensureAccessibleDir(DIRS.results)"
      pattern: "DIRS\\.results\\s*=\\s*ensureAccessibleDir"
    - from: "fork:pi-subagents/src/runs/background/async-execution.ts"
      to: "fork:pi-subagents/src/shared/types.ts"
      via: "uses DIRS.async and DIRS.results instead of RESULTS_DIR/ASYNC_DIR"
      pattern: "DIRS\\.(async|results)"
---

<objective>
Author and submit an upstream PR to pi-subagents that fixes the EPERM crash (per D-01) and enables fallback path propagation via a mutable DIRS container (per D-02). This is the primary structural fix; it must go upstream as decided in D-03.

Purpose: The EPERM crash is a startup blocker on Windows Azure AD machines. Without this fix, pi-subagents cannot load when NTFS ACLs are corrupted. The ES module read-only binding prevents fallback paths from reaching consumer modules. Both must be fixed in upstream code.

Output: A PR against nicobailon/pi-subagents containing (1) EPERM-tolerant `ensureAccessibleDir` with pid-scoped fallback, (2) DIRS mutable container replacing `export const RESULTS_DIR`/`export const ASYNC_DIR`, (3) all consumer module migrations.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-subagent-stability/03-CONTEXT.md
@.planning/phases/03-subagent-stability/03-RESEARCH.md
@docs/issue-pi-subagents-eperm.md
@node_modules/pi-subagents/src/extension/index.ts
@node_modules/pi-subagents/src/shared/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EPERM-tolerant ensureAccessibleDir with pid-scoped fallback</name>
  <files>fork:pi-subagents/src/extension/index.ts</files>
  <read_first>
    node_modules/pi-subagents/src/extension/index.ts (lines 89-101 for current ensureAccessibleDir, lines 220-230 for call sites at RESULTS_DIR/ASYNC_DIR)
    .planning/phases/03-subagent-stability/03-RESEARCH.md (Pattern 1: recommended fix pattern with full code example)
    docs/issue-pi-subagents-eperm.md (root cause analysis sections 2 and 3)
  </read_first>
  <behavior>
    - Test: ensureAccessibleDir returns the original path when mkdir succeeds and access check passes
    - Test: ensureAccessibleDir catches EPERM from mkdirSync, attempts rmSync+retry, returns original path on retry success
    - Test: ensureAccessibleDir returns `${dirPath}-${process.pid}` fallback when both mkdir and retry fail with EPERM/EACCES
    - Test: ensureAccessibleDir does NOT catch non-EPERM/EACCES errors (e.g., ENOSPC re-throws)
    - Test: ensureAccessibleDir returns fallback when accessSync fails after successful mkdir, and rm+retry also fails
  </behavior>
  <action>
    Fork nicobailon/pi-subagents on GitHub. Clone the fork locally. Create branch `fix/eperm-ensure-accessible-dir`.

    Modify `ensureAccessibleDir` in `src/extension/index.ts` per D-01 and RESEARCH Pattern 1:
    Change return type from `void` to `string`. Wrap the first `fs.mkdirSync(dirPath, { recursive: true })` in a try/catch that catches only `err.code === 'EPERM' || err.code === 'EACCES'`. On catch: attempt `fs.rmSync(dirPath, { recursive: true, force: true })` (best effort, catch and ignore). Attempt `fs.mkdirSync(dirPath, { recursive: true })` again. If retry also throws EPERM/EACCES, compute fallback as `` `${dirPath}-${process.pid}` ``, call `fs.mkdirSync(fallback, { recursive: true })`, verify with `fs.accessSync(fallback, fs.constants.R_OK | fs.constants.W_OK)`, and return fallback. Keep the existing accessSync verification block after successful initial mkdir — same fallback logic if access check fails. If no fallback needed, return `dirPath`.

    Update call sites at lines 226-227 from:
    `ensureAccessibleDir(RESULTS_DIR); ensureAccessibleDir(ASYNC_DIR);`
    to:
    `DIRS.results = ensureAccessibleDir(DIRS.results); DIRS.async = ensureAccessibleDir(DIRS.async);`

    This assignment depends on the DIRS container from Task 2. If implementing sequentially, temporarily use local variables and wire to DIRS after Task 2 lands. Do NOT use `let RESULTS_DIR` or `let ASYNC_DIR` reassignment — ES module `export const` bindings are read-only and cannot be reassigned from importers.
  </action>
  <verify>
    <automated>cd D:/Workstation/pi-subagents && npx vitest run --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|ensureAccessibleDir)"</automated>
  </verify>
  <done>ensureAccessibleDir catches EPERM/EACCES, attempts recovery, returns pid-scoped fallback when recovery fails, re-throws non-EPERM errors. Call sites use DIRS.results/DIRS.async assignment. All existing pi-subagents tests pass plus new EPERM tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DIRS mutable container and migrate all consumer modules</name>
  <files>
    fork:pi-subagents/src/shared/types.ts
    fork:pi-subagents/src/extension/index.ts
    fork:pi-subagents/src/extension/doctor.ts
    fork:pi-subagents/src/runs/background/async-execution.ts
    fork:pi-subagents/src/runs/background/async-job-tracker.ts
    fork:pi-subagents/src/runs/background/async-resume.ts
    fork:pi-subagents/src/runs/background/run-id-resolver.ts
    fork:pi-subagents/src/runs/background/run-status.ts
    fork:pi-subagents/src/runs/background/stale-run-reconciler.ts
    fork:pi-subagents/src/runs/shared/nested-events.ts
  </files>
  <read_first>
    node_modules/pi-subagents/src/shared/types.ts (lines 670-685 for RESULTS_DIR, ASYNC_DIR, TEMP_ROOT_DIR, CHAIN_RUNS_DIR, TEMP_ARTIFACTS_DIR definitions)
    .planning/phases/03-subagent-stability/03-RESEARCH.md (Pattern 2: DIRS container, complete consumer audit with file/line references)
    docs/issue-pi-subagents-eperm.md (section 4: ES Module read-only binding limitation)
  </read_first>
  <acceptance_criteria>
    - types.ts exports `DIRS` object with `results`, `async`, `chain`, `artifacts` mutable properties
    - Backward-compatible `RESULTS_DIR` and `ASYNC_DIR` aliases (reading from DIRS) remain for any undiscovered consumers
    - All 4 direct-import consumer files updated to use `DIRS.results`/`DIRS.async` instead of `RESULTS_DIR`/`ASYNC_DIR`
    - All 7 DI-injected consumer files updated to use `DIRS.results`/`DIRS.async` as the default fallback
    - No remaining `import { RESULTS_DIR }` or `import { ASYNC_DIR }` in any file that uses the constant for path construction (import for re-export or backward-compat alias is acceptable)
    - All pi-subagents existing tests pass
    - New test: assigning `DIRS.results = "/new/path"` propagates when read by a consumer
  </acceptance_criteria>
  <action>
    Per D-02 and RESEARCH Pattern 2: In `src/shared/types.ts`, replace the individual `export const RESULTS_DIR` and `export const ASYNC_DIR` with a mutable `DIRS` container object:

    Export `DIRS` as: `export const DIRS = { results: RESULTS_DIR, async: ASYNC_DIR, chain: CHAIN_RUNS_DIR, artifacts: TEMP_ARTIFACTS_DIR };` where the initial values come from the existing path computations. The `export const DIRS` binding is read-only (can't reassign `DIRS` itself) but the object's properties are mutable (can reassign `DIRS.results`).

    Also keep backward-compatible aliases: `export const RESULTS_DIR = DIRS.results;` and `export const ASYNC_DIR = DIRS.async;` These are evaluated once at import time and won't track DIRS mutations, but they prevent breakage for any undiscovered consumers that import the constants directly. Add a comment noting these are static snapshots for backward compat only.

    Migrate consumer modules per the RESEARCH consumer audit:
    - `src/extension/index.ts` lines 226-227: already updated in Task 1 to use `DIRS.results = ensureAccessibleDir(DIRS.results)` and `DIRS.async = ensureAccessibleDir(DIRS.async)`. Also update the import at line 45 to import `DIRS` alongside `RESULTS_DIR`/`ASYNC_DIR`.
    - `src/extension/index.ts` line 258: `RESULTS_DIR` passed to `createResultWatcher` — this is a parameterized call so it reads the current value; change to `DIRS.results` so it reads the possibly-overridden value.
    - `src/extension/index.ts` line 274: `ASYNC_DIR` passed to `createAsyncJobTracker` — same, change to `DIRS.async`.
    - `src/extension/doctor.ts` lines 47-48: change `asyncDir: ASYNC_DIR, resultsDir: RESULTS_DIR` to `asyncDir: DIRS.async, resultsDir: DIRS.results`.
    - `src/runs/background/async-execution.ts` lines 273, 405, 571, 623: change all `ASYNC_DIR` to `DIRS.async` and `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/background/async-job-tracker.ts` line 37: change default fallback `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/background/async-resume.ts` lines 240-241: change `ASYNC_DIR` to `DIRS.async` and `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/background/run-id-resolver.ts` lines 56-57: change `ASYNC_DIR` to `DIRS.async` and `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/background/run-status.ts` lines 102-103: change `ASYNC_DIR` to `DIRS.async` and `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/background/stale-run-reconciler.ts` lines 257, 303: change `RESULTS_DIR` to `DIRS.results`.
    - `src/runs/shared/nested-events.ts` lines 812, 818: change `ASYNC_DIR` to `DIRS.async` and `RESULTS_DIR` to `DIRS.results`.

    After all migrations, remove `RESULTS_DIR` and `ASYNC_DIR` from the import statements in files that no longer use them directly, keeping them only in `types.ts` for backward compatibility. Add `DIRS` to the import statements where needed.

    Commit with message: `feat: replace export const RESULTS_DIR/ASYNC_DIR with mutable DIRS container` referencing that this enables EPERM fallback path propagation.

    Push the branch and open a PR against `nicobailon/pi-subagents` main branch with title "Fix EPERM crash on Windows ACL corruption + enable DIRS mutable container for fallback paths" and a description summarizing both fixes, referencing the issue doc.

    Per D-03: if the PR is accepted, no fork is needed. If upstream is unresponsive after 2 weeks, the fork at `NOirBRight/pi-subagents` with these changes becomes the fallback. Do NOT publish the fork as an npm package yet — that decision is deferred until the 2-week window passes.
  </action>
  <verify>
    <automated>cd D:/Workstation/pi-subagents && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Upstream PR submitted with both fixes. DIRS container works. All 11 consumer sites migrated. All pi-subagents tests pass. PR URL recorded for D-03 tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pi-gsd-redux → pi-subagents fork | pi-gsd-redux may later consume a forked pi-subagents package; trust boundary is npm package integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pid-scoped fallback directory name | accept | process.pid is kernel-assigned and non-sequential on Windows; low value for temp file spoofing |
| T-03-02 | Denial of Service | fallback dir accumulation without cleanup | mitigate | Include cleanup for `dirPath-*` pattern in PR; existing cleanup logic extended |
| T-03-03 | Tampering | npm package substitution if fork published | mitigate | If fork is published, use scoped name `@noirbright/pi-subagents` to prevent name collision |
| T-03-SC | Tampering | npm install of pi-subagents | accept | Package legitimacy audit in RESEARCH.md: pi-subagents [OK], no new packages installed |
</threat_model>

<verification>
- Fork repo has branch with both fixes
- ensureAccessibleDir catches EPERM/EACCES and returns fallback
- DIRS container is exported and all consumers migrated
- PR URL is accessible
- All pi-subagents existing tests pass + new EPERM tests pass
</verification>

<success_criteria>
- ensureAccessibleDir in fork returns string (not void) and catches EPERM with pid-scoped fallback
- DIRS object exported from types.ts with mutable results/async/chain/artifacts properties
- All 11 consumer module sites use DIRS.* instead of direct RESULTS_DIR/ASYNC_DIR
- PR submitted against nicobailon/pi-subagents
- All pi-subagents tests pass (existing + new)
</success_criteria>

<output>
Create `.planning/phases/03-subagent-stability/03-01-SUMMARY.md` when done
</output>