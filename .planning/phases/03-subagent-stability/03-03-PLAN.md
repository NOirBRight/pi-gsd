---
phase: 03-subagent-stability
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - tests/eperm-guard.test.ts
  - package.json
autonomous: false
requirements: [D-04]

must_haves:
  truths:
    - "All 180 pi-gsd-redux tests still pass after subagent stability changes"
    - "doctor --agents passes and includes ACL corruption check"
    - "/gsd-models command works in a fresh Pi session after changes"
    - "If upstream PR accepted and published, pi-gsd-redux uses the fixed version"
    - "If upstream PR not accepted, pi-gsd-redux interim guard is in place and fork decision is documented"
  artifacts:
    - path: "tests/eperm-guard.test.ts"
      provides: "Integration tests for EPERM guard behavior"
      min_lines: 30
    - path: "package.json"
      provides: "pi-subagents dependency with correct version (upstream fixed or fork)"
      contains: "pi-subagents"
  key_links:
    - from: "tests/eperm-guard.test.ts"
      to: "src/extension.ts"
      via: "imports guardPiSubagentsTempDirs and buildPiSubagentsTempRoot"
      pattern: "guardPiSubagentsTempDirs|buildPiSubagentsTempRoot"
---

<objective>
Verify that all pi-gsd-redux functionality works correctly after the subagent stability changes. Per D-04: run full npm run check plus manual /gsd-models invocation in a fresh Pi session. If the upstream PR from Plan 01 has been accepted and published, update pi-subagents dependency to the fixed version. If not, verify the interim guard from Plan 02 is functional.

Purpose: No fix is complete until it's verified. The EPERM changes (upstream PR + interim guard) must not break existing pi-gsd-redux functionality: /gsd-models, doctor, agent sync, runtime rewrites. This is the D-04 gate.

Output: Green test suite, green doctor, verified /gsd-models, documented upstream PR status.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-subagent-stability/03-CONTEXT.md
@.planning/phases/03-subagent-stability/03-RESEARCH.md
@.planning/phases/03-subagent-stability/03-01-SUMMARY.md
@.planning/phases/03-subagent-stability/03-02-SUMMARY.md
@src/extension.ts
@src/doctor.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EPERM integration tests and run full verification suite</name>
  <files>tests/eperm-guard.test.ts, package.json</files>
  <read_first>
    tests/extension.test.ts (existing test patterns for extension functions)
    tests/doctor.test.ts (existing test patterns for doctor checks)
    src/extension.ts (guardPiSubagentsTempDirs, buildPiSubagentsTempRoot, TEMP_DIR_SUBDIRS exports)
    src/doctor.ts (checkPiSubagentsTempAcl function)
    package.json (current pi-subagents version dependency)
    .planning/phases/03-subagent-stability/03-01-SUMMARY.md (upstream PR status: accepted/rejected/pending)
  </read_first>
  <acceptance_criteria>
    - tests/eperm-guard.test.ts exists and tests guard + end-to-end EPERM scenarios
    - npm run typecheck exits 0
    - npx vitest run passes all tests (180+)
    - npm run build succeeds
    - node dist/cli.js doctor --prompts generated/prompts --agents --cwd . exits 0
    - pi-subagents dependency in package.json points to the fixed upstream version if PR was accepted and published
  </acceptance_criteria>
  <action>
    Read Plan 01 SUMMARY to determine upstream PR status. If PR was accepted and a new pi-subagents version was published with the fix: update `package.json` `dependencies.pi-subagents` to the new version. Run `npm install` to update node_modules. If PR is still pending and no new version published: verify the interim guard from Plan 02 is functional and document the status.

    Create `tests/eperm-guard.test.ts` with integration tests:
    1. Test that `buildPiSubagentsTempRoot` returns a path containing `pi-subagents-user-`
    2. Test that `TEMP_DIR_SUBDIRS` contains the expected subdirectory names
    3. Test that `guardPiSubagentsTempDirs` does not throw when temp dirs are accessible (use vitest `vi.spyOn` on `fs.accessSync` to simulate accessible)
    4. Test that `guardPiSubagentsTempDirs` attempts repair when `accessSync` throws EACCES (spy on `fs.rmSync` and `fs.mkdirSync`)
    5. Test that `guardPiSubagentsTempDirs` sets the global flag when repair also fails
    6. Test that `checkPiSubagentsTempAcl` from doctor.ts reports ACL ok when dirs are accessible (via runDoctor with appropriate mocks)

    Use vitest mocking conventions from existing test files — import `vi` from vitest, use `vi.spyOn` and `vi.restoreAllMocks` in beforeEach/afterEach.

    Run full verification: `npm run typecheck && npx vitest run && npm run build && node dist/cli.js doctor --prompts generated/prompts --agents --cwd .`

    Record PR status and verification results in the SUMMARY.
  </action>
  <verify>
    <automated>cd "D:/Workstation/pi-gsd" && npm run check 2>&1 | tail -15</automated>
  </verify>
  <done>All tests pass (180+). Typecheck clean. Build succeeds. Doctor passes. EPERM guard integration tests exist and pass. Upstream PR status documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify /gsd-models in a fresh Pi session</name>
  <files></files>
  <what-built>
    Interim EPERM guard in pi-gsd-redux extension, ACL diagnostic in doctor command, and optionally an updated pi-subagents dependency from Plan 01.
  </what-built>
  <how-to-verify>
    1. Close any running Pi sessions
    2. Start a fresh Pi session in the pi-gsd-redux project directory
    3. Type `/gsd-models` in the Pi chat
    4. Verify the interactive model selector appears with all 5 profiles (Inherit, Quality, Balanced, Budget, Adaptive)
    5. Select a profile and verify models are displayed
    6. Run `/gsd-models` again and verify previous selection is persisted
    7. Also verify that pi-subagents extension loaded without EPERM errors (check Pi's extension loading output)
    8. If on Windows, verify temp directories at %TEMP%\pi-subagents-user-{username}\ are accessible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pi-gsd-redux → pi-subagents (updated version) | Dependency update may introduce new code; verify via npm audit and test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | Updated pi-subagents package with malicious code | mitigate | Verify npm audit passes; check published version matches PR source; run full test suite |
| T-03-SC | Tampering | npm install supply chain | accept | No new packages; only pi-subagents version update if upstream PR accepted |
</threat_model>

<verification>
- npm run check passes (typecheck + tests + build + doctor)
- /gsd-models works in fresh Pi session (human-verified)
- pi-subagents extension loads without EPERM
- Doctor includes ACL check line
</verification>

<success_criteria>
- All tests pass (180+)
- npm run check exits 0
- /gsd-models interactive flow works end-to-end in Pi session
- pi-subagents loads without crash
- Upstream PR status documented (accepted, pending, or fork-needed)
- If fork needed: decision documented with 2-week deadline note
</success_criteria>

<output>
Create `.planning/phases/03-subagent-stability/03-03-SUMMARY.md` when done
</output>