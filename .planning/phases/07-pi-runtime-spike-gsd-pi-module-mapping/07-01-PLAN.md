---
phase: 07-pi-runtime-spike-gsd-pi-module-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md
  - .planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md
  - .planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md
autonomous: true
requirements:
  - RUNTIME-01
  - UPSTREAM-03
  - UPSTREAM-04
  - STATE-03
  - ORCH-01

must_haves:
  truths:
    - "Pi argv-passing mechanism ($ARGUMENTS substitution) is documented with a verified reproducer tracing the full expandPromptTemplate → parseCommandArgs → substituteArgs chain"
    - "All five gsd-pi anchor modules (Auto Orchestration, State Reconciliation, Worktree Safety, Recovery Classification, Tool Contract) are mapped to v2.0 surfaces with mirror decisions, ADR references, and file-level source citations under packages/daemon/src/ (per D-08)"
    - "Upstream 1.2.0 gsd-tools.cjs CLI surface (30+ commands), gsd_run launcher convention (4 fallback paths), and DispatchLogger seam (event shape, wiring point, audit format) are cataloged as an API surface reference"
    - "Phase 8 upgrade can proceed with a known changeset — no re-investigation of upstream surface required"
  artifacts:
    - path: ".planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md"
      provides: "Verified Pi $ARGUMENTS substitution contract with reproducer"
      contains: "parseCommandArgs"
      contains: "substituteArgs"
      contains: "expandPromptTemplate"
      contains: "$ARGUMENTS"
      min_sections: 4
    - path: ".planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md"
      provides: "5-module gsd-pi-to-v2.0 mapping table with mirror/defer/N/A decisions"
      contains: "Auto Orchestration"
      contains: "State Reconciliation"
      contains: "ADR-009"
      contains: "packages/daemon/src"
      min_sections: 5
    - path: ".planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md"
      provides: "1.2.0 API surface catalog for Phase 8 migration"
      contains: "gsd-tools.cjs"
      contains: "gsd_run"
      contains: "DispatchLogger"
      contains: "createDefaultLogger"
      min_sections: 5
  key_links:
    - from: "spike/pi-argv.md"
      to: "node_modules/@earendil-works/pi-coding-agent/dist/core/prompt-templates.js"
      via: "source inspection"
      pattern: "parseCommandArgs|substituteArgs|expandPromptTemplate"
    - from: "spike/gsd-pi-module-map.md"
      to: "D:\\Workstation\\gsd-pi-fork/packages/daemon/src/"
      via: "ADR surface + file-level references"
      pattern: "ADR-009|ADR-014|ADR-017"
    - from: "spike/upstream-1.2.0-impact.md"
      to: "@opengsd/gsd-core@1.2.0 get-shit-done/bin/"
      via: "npm pack extraction"
      pattern: "gsd-tools\\.cjs|_runtime-launcher\\.snippet\\.sh|observability"
---

<objective>
Produce three research/spike artifacts that establish the technical foundation for v2.0 Runtime Refactor (Phases 8-13):

1. **pi-argv.md** — Verified Pi argv-passing mechanism: documents the `expandPromptTemplate` → `parseCommandArgs` → `substituteArgs` chain in `prompt-templates.js`, all 6 substitution patterns (`$1`, `$2`, `${@:N}`, `${@:N:L}`, `$ARGUMENTS`, `$@`), and a step-by-step reproducer showing `/gsd-plan-phase 07 --chain` arg delivery (per D-05; RUNTIME-01).

2. **gsd-pi-module-map.md** — Maps all five gsd-pi anchor modules to v2.0 surfaces with mirror decisions, ADR references (ADR-009, ADR-014, ADR-015, ADR-016, ADR-017), file-level source citations under `packages/daemon/src/` (per D-07, D-08), interface contracts, and deferred/N/A decisions. All five mirror in v2.0 (per D-09); supports STATE-03 and ORCH-01 architecture design.

3. **upstream-1.2.0-impact.md** — Catalogs the 1.2.0 API surface: gsd-tools.cjs CLI (30+ atomic commands across 8 families), gsd_run launcher convention (shell function with 4 fallback paths), DispatchLogger seam (event shape in `event.cjs`, wiring point in `command-routing-hub.cjs`, audit format in `logger.cjs`), and assessment of suitability for v2.0 Auto Orchestration observability (per D-03, D-04, D-06; UPSTREAM-03, UPSTREAM-04).

Purpose: Feed Phase 8 upgrade planning with a known upstream surface catalog so the upgrade proceeds without re-investigation. Feed Phases 9-12 with verified Pi runtime contract and gsd-pi module blueprints.

Output: Three Markdown files in `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/`. No production code changes.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/07-CONTEXT.md
@.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/07-RESEARCH.md

## Source Files for Verification (Task 1 — pi-argv.md)
@node_modules/@earendil-works/pi-coding-agent/dist/core/prompt-templates.js

## Source Files for Verification (Task 2 — gsd-pi-module-map.md)
@D:\Workstation\gsd-pi-fork/docs/dev/ADR-009-orchestration-kernel-refactor.md
@D:\Workstation\gsd-pi-fork/docs/dev/ADR-014-auto-orchestration-deep-module.md
@D:\Workstation\gsd-pi-fork/docs/dev/ADR-015-runtime-invariant-modules.md
@D:\Workstation\gsd-pi-fork/docs/dev/ADR-016-worktree-safety-fail-closed.md
@D:\Workstation\gsd-pi-fork/docs/dev/ADR-017-state-reconciliation-drift-driven.md
@D:\Workstation\gsd-pi-fork/packages/daemon/src/orchestrator.ts
@D:\Workstation\gsd-pi-fork/src/resources/extensions/gsd/state-reconciliation/index.ts
@D:\Workstation\gsd-pi-fork/src/resources/extensions/gsd/recovery-classification.ts
@D:\Workstation\gsd-pi-fork/src/resources/extensions/gsd/tool-contract.ts
@D:\Workstation\gsd-pi-fork/src/resources/extensions/gsd/worktree-safety.ts
@D:\Workstation\gsd-pi-fork/src/resources/extensions/gsd/auto/orchestrator.ts

## Source Files for Verification (Task 3 — upstream-1.2.0-impact.md)
Execute: `npm pack @opengsd/gsd-core@1.2.0 --pack-destination /tmp/gsd120-pack` then extract tarball
Read: `/tmp/gsd120-pack/package/get-shit-done/bin/gsd-tools.cjs`
Read: `/tmp/gsd120-pack/package/get-shit-done/workflows/_runtime-launcher.snippet.sh`
Read: `/tmp/gsd120-pack/package/get-shit-done/bin/lib/observability/event.cjs`
Read: `/tmp/gsd120-pack/package/get-shit-done/bin/lib/observability/logger.cjs`
Read: `/tmp/gsd120-pack/package/get-shit-done/bin/lib/observability/redaction.cjs`
Read: `/tmp/gsd120-pack/package/get-shit-done/bin/lib/command-routing-hub.cjs`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce pi-argv.md — Verified Pi $ARGUMENTS Substitution Contract</name>
  <files>.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md</files>
  <read_first>
  Read `node_modules/@earendil-works/pi-coding-agent/dist/core/prompt-templates.js` in full (251 lines). Focus on three functions: `parseCommandArgs` (line ~18, bash-style arg parser), `substituteArgs` (line ~58, 6 pattern substitutions), `expandPromptTemplate` (line ~240, orchestrates the full flow). Also re-read RESEARCH.md §Pi Argv-Passing Mechanism for the verified flow analysis and §Common Pitfalls #1 for the env-var vs template-substitution distinction.
  </read_first>
  <action>
  Write `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md` with the following structure:

  1. **Contract Summary** — One-paragraph statement: Pi performs literal string substitution on template content before the LLM sees it. `$ARGUMENTS` / `$@` / `$1` / `$2` / `${@:N}` / `${@:N:L}` are template placeholders, not runtime variables. The LLM never sees raw placeholders.

  2. **Verified Flow** — Document the three-stage pipeline with function names and source locations:
     - Stage 1: `parseCommandArgs(argsString)` → bash-style parsing → `string[]`. Note: handles quoted strings (`"` and `'`), whitespace delimiting.
     - Stage 2: `expandPromptTemplate(text, templates)` → template resolution. Shows regex `^\/([^\s]+)(?:\s+([\s\S]*))?$` extracts template name and raw args string.
     - Stage 3: `substituteArgs(content, args)` → 6 patterns applied in order: `$1`/`$2`... positional (first, before wildcards to prevent re-substitution), `${@:start:length}` slicing, `${@:start}` slicing, `$ARGUMENTS` (all args joined), `$@` (legacy, all args joined).

  3. **Substitution Patterns Table** — 6-row table: Pattern | Input Example | Output Example | Notes. Use `/gsd-plan-phase 07 --chain` as the canonical example (args: `["07", "--chain"]`).

  4. **Reproducer** — A concrete step-by-step:
     a. Locate `prompt-templates.js` in `node_modules/@earendil-works/pi-coding-agent/dist/core/`
     b. Identify the template for a known GSD workflow (e.g., `/gsd-plan-phase`) in `.pi/prompts/` or `commands/gsd/`
     c. Show the template content before substitution (where `$ARGUMENTS` appears in the markdown)
     d. Trace `parseCommandArgs("07 --chain")` → `["07", "--chain"]`
     e. Trace `substituteArgs(templateContent, ["07", "--chain"])` → content with `$ARGUMENTS` replaced by `07 --chain`, `$1` replaced by `07`
     f. Confirm: the LLM receives the template content with all placeholders substituted

  5. **v2.0 Implications** — Per D-05 (Pi side only, scope boundary):
     - `--chain` and `--auto` flags arrive as literal strings in substituted prompt content
     - pi-gsd-redux's Auto Orchestration (Phase 9) must detect these flags in prompt content to trigger native orchestration
     - The substitution contract is Pi-owned — pi-gsd-redux cannot change it
     - `AUTO_MODE_CHECKLIST` is injected into template content before Pi substitution happens
     - Cross-reference D-06: gsd-tools calling conventions are in upstream-1.2.0-impact.md, NOT here

  6. **Source References** — Link to `node_modules/@earendil-works/pi-coding-agent/dist/core/prompt-templates.js` with line numbers for each function.

  Do NOT include: gsd-tools CLI analysis, upstream package inspection results, or gsd-pi module mapping content (those are Task 2 and Task 3 scope per D-06).
  </action>
  <acceptance_criteria>
  <behavior>The file documents the complete `expandPromptTemplate` → `parseCommandArgs` → `substituteArgs` chain with function names, source locations, and all 6 substitution patterns.</behavior>
  <behavior>The reproducer section demonstrates `/gsd-plan-phase 07 --chain` arg delivery with concrete input/output at each stage.</behavior>
  <behavior>The v2.0 implications section correctly scopes to Pi side only (no gsd-tools content, per D-06).</behavior>
  <behavior>The substitution patterns table covers all 6 patterns: `$1`, `$2`, `${@:N}`, `${@:N:L}`, `$ARGUMENTS`, `$@`.</behavior>
  </acceptance_criteria>
  <verify>
    <automated>Read `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md` — confirm all 6 substitution patterns are documented, reproducer steps are concrete, and no gsd-tools content appears (per D-06 boundary).</automated>
  </verify>
  <done>pi-argv.md exists at `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/pi-argv.md` with ≥4 sections, covers all 6 substitution patterns, includes a concrete reproducer, correctly scopes to Pi side only (per D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Produce gsd-pi-module-map.md — 5-Module Mapping with ADR Surface References</name>
  <files>.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md</files>
  <read_first>
  Re-read RESEARCH.md §gsd-pi Module Mapping for the complete module map table, mirror/defer/N/A decisions, deferred decisions table, and module organization vs v2.0 target table. Re-read RESEARCH.md §Architecture Patterns for the 4 patterns to follow (Deep Module with Small Interface, Typed Discriminated Unions, Idempotent Repair with Detection Loop, anti-patterns to avoid). For source verification during writing, read from `D:\Workstation\gsd-pi-fork`: ADR-009 (orchestration kernel), ADR-014 (auto orchestration deep module), ADR-015 (runtime invariants with advance() pipeline ordering), ADR-016 (worktree safety fail-closed), ADR-017 (state reconciliation drift-driven), plus implementation files: `packages/daemon/src/orchestrator.ts`, `src/resources/extensions/gsd/state-reconciliation/index.ts`, `src/resources/extensions/gsd/recovery-classification.ts`, `src/resources/extensions/gsd/tool-contract.ts`, `src/resources/extensions/gsd/worktree-safety.ts`, `src/resources/extensions/gsd/auto/orchestrator.ts`.
  </read_first>
  <action>
  Write `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md` with the following structure:

  1. **Reference Source** — Per D-07: gsd-pi fork at `D:\Workstation\gsd-pi-fork` (v1.0.2), commit `fc39cdcdd` (2026-05-25). State the 5 ADR anchors (ADR-009, ADR-014, ADR-015, ADR-016, ADR-017).

  2. **Module Map Table** — 5-row table, one per v2.0 anchor module, with columns:
     - v2.0 Module (Auto Orchestration, State Reconciliation, Worktree Safety, Recovery Classification, Tool Contract)
     - Phase (9, 10, 11, 11, 12)
     - gsd-pi ADR Surface (cite specific ADR + section)
     - gsd-pi File References (per D-08: cite under `packages/daemon/src/` and `src/resources/extensions/gsd/` — NOT the non-existent `extensions/gsd/{auto,state-reconciliation,safety}/` path)
     - Mirror Decision (all 5 = **Mirror** per D-09)
     - Interface Contract (the public API surface: `start/advance/resume/stop/getStatus` for Auto Orchestration; `reconcileBeforeDispatch` for State Reconciliation; `prepareUnitRoot` for Worktree Safety; `classifyFailure` for Recovery Classification; `compileUnitToolContract` for Tool Contract)

  3. **Per-Module Deep Dives** — One subsection per module (5 total):
     - **Auto Orchestration (Phase 9):** ADR-009 6-plane UOK + ADR-014 deep module. Interface: `start(sessionContext)`, `advance()`, `resume()`, `stop(reason)`, `getStatus()`. Invariants from ADR-014: exactly one active unit, idempotent advance, lock ownership validation, no skipping verification transitions, every state transition journaled. File reference: `auto/orchestrator.ts` (415 lines). Per ADR-015, the `advance()` pipeline ordering is: State Reconciliation → Dispatch Decision → Tool Contract → Worktree Safety → Runtime persistence.
     - **State Reconciliation (Phase 10):** ADR-017 drift-driven. Interface: `reconcileBeforeDispatch(basePath, deps)` → `{ ok, stateSnapshot, repaired, blockers }`. 2-pass detection-repair loop (MAX_PASSES=2). Drift catalog: 7 typed `DriftRecord` variants (stale-sketch-flag, unmerged-merge-state, stale-worker, unregistered-milestone, roadmap-divergence, missing-completion-timestamp, cascading). File reference: `state-reconciliation/index.ts` (109 lines).
     - **Worktree Safety (Phase 11):** ADR-016 fail-closed. Interface: `prepareUnitRoot(unitType, unitId)` → valid root or typed `worktree-invalid`. Checks: `.git` exists, branch matches expected, lease ownership current, `GSD_PROJECT_ROOT` match. No silent degradation to project root. File reference: `worktree-safety.ts` (327+ lines).
     - **Recovery Classification (Phase 11):** ADR-015 runtime invariants. Interface: `classifyFailure(input)` → `{ failureKind, action, reason, exitReason, remediation }`. 8 explicit `RecoveryFailureKind` values: tool-schema, deterministic-policy, stale-worker, worktree-invalid, provider-quota (called "provider" in gsd-pi), network ("runtime-unknown" catch-all in gsd-pi), verification-drift, reconciliation-drift. Each maps to one action: retry / pause-with-remediation / self-heal / stop. File reference: `recovery-classification.ts` (139 lines).
     - **Tool Contract (Phase 12):** ADR-015 runtime invariants. Interface: `compileUnitToolContract(unitType)` → `{ ok, contract }` with prompt obligations, allowed tools, schema enum values, validation requirements, closeout tools. Validation runs BEFORE model turn launch. File reference: `tool-contract.ts` (84 lines).

  4. **advance() Pipeline Ordering** — Per RESEARCH.md §Architecture Patterns: the canonical ordering from ADR-015: State Reconciliation → Dispatch Decision → Tool Contract → Worktree Safety → Runtime persistence. This is the fixed, enforced sequence the orchestrator calls. Each step gates the next.

  5. **Deferred / N/A Decisions Table** — 5-row table (parallel slice orchestrator → defer to v2.1; Cloud MCP Gateway → N/A; Discord bot → N/A; worktree lifecycle projection → defer to v2.1; gsd-pi prompts → N/A). Each row has rationale per RESEARCH.md.

  6. **Module Organization vs v2.0 Target** — 5-row table mapping gsd-pi source paths to pi-gsd-redux v2.0 target paths (e.g., `src/resources/extensions/gsd/auto/orchestrator.ts` → `src/orchestrator/`).

  7. **Architecture Patterns to Follow** — 4 patterns from RESEARCH.md §Architecture Patterns: Deep Module with Small Interface, Typed Discriminated Unions, Idempotent Repair with Detection Loop, plus the 4 anti-patterns to avoid.

  All five modules = Mirror (per D-09). No deferrals within the 5 anchor modules. The 5 gsd-pi file references MUST cite actual file paths under `packages/daemon/src/` and `src/resources/extensions/gsd/` per D-08 — never reference the non-existent `extensions/gsd/{auto,state-reconciliation,safety}/` path from the original ROADMAP draft.
  </action>
  <acceptance_criteria>
  <behavior>The module map table covers all 5 v2.0 modules with ADR references, file-level citations under packages/daemon/src/ (per D-08), mirror decisions (all = Mirror per D-09), and interface contracts.</behavior>
  <behavior>Each per-module deep dive section cites the specific gsd-pi ADR, the public API interface signature, and implementation file location with line count.</behavior>
  <behavior>The advance() pipeline ordering section documents the ADR-015 canonical sequence: State Reconciliation → Dispatch Decision → Tool Contract → Worktree Safety → Runtime persistence.</behavior>
  <behavior>The deferred/N/A table covers all 5 gsd-pi surfaces that are NOT mirrored: parallel slice orchestrator (defer to v2.1), Cloud MCP Gateway (N/A), Discord bot (N/A), worktree lifecycle projection (defer to v2.1), gsd-pi prompts (N/A).</behavior>
  <behavior>No reference to the non-existent `extensions/gsd/{auto,state-reconciliation,safety}/` path (per D-08).</behavior>
  </acceptance_criteria>
  <verify>
    <automated>Read `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md` — confirm 5 module rows, all = Mirror, ADR citations present, file paths under packages/daemon/src/ (not extensions/gsd/ per D-08), deferred/N/A table covers 5 non-mirrored surfaces, advance() pipeline ordering documented.</automated>
  </verify>
  <done>gsd-pi-module-map.md exists at `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/gsd-pi-module-map.md` with ≥5 sections, all 5 modules mapped to v2.0 with Mirror decisions (per D-09), ADR references, and correct file paths (per D-08).</done>
</task>

<task type="auto">
  <name>Task 3: Produce upstream-1.2.0-impact.md — API Surface Catalog for Phase 8 Migration</name>
  <files>.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md</files>
  <read_first>
  Re-read RESEARCH.md §Upstream 1.2.0 Impact for the structural changes table, gsd-tools CLI surface command families, gsd_run launcher convention, and DispatchLogger seam assessment. Also re-read RESEARCH.md §Common Pitfalls #2 (treating gsd-tools as SDK replacement) and #4 (skipping runtime-launcher resolution logic). Re-read RESEARCH.md §State of the Art for old-vs-new approach comparison. Re-read CONTEXT.md D-03 (code-level inspection, not release notes), D-04 (not full diff — that's Phase 8), D-06 (gsd-tools/gsd_run in this artifact, not pi-argv.md).

  For source verification during writing:
  1. Run `npm pack @opengsd/gsd-core@1.2.0 --pack-destination /tmp/gsd120-pack` and extract the tarball
  2. Read `gsd-tools.cjs` for the CLI command list (confirm 30+ commands across 8 families: atomic, phase, roadmap, requirements, milestone, validation, progress, uat/intel)
  3. Read `_runtime-launcher.snippet.sh` for the gsd_run shell function with 4 fallback paths
  4. Read `observability/event.cjs` for `makeDispatchEvent` with `traceId`, `parentTraceId`, result shape
  5. Read `observability/logger.cjs` for `createDefaultLogger` / `createNoOpLogger` with audit file and stderr behaviors
  6. Read `observability/redaction.cjs` for arg redaction policy (`GSD_AUDIT_ARGS` opt-in)
  7. Read `command-routing-hub.cjs` for the logger injection seam (`logger.onEvent` wiring point)
  </read_first>
  <action>
  Write `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md` with the following structure:

  1. **Executive Summary** — 3-sentence summary: (a) Package renamed from `@opengsd/get-shit-done-redux` to `@opengsd/gsd-core`, (b) SDK directory (`sdk/dist/query/*.js`) removed entirely — pi-gsd-redux's 91-route `gsd_query` bridge is broken at the import level, (c) New surfaces: `gsd-tools.cjs` CLI (30+ commands), `gsd_run()` shell launcher, `DispatchLogger` observability seam.

  2. **Structural Changes from 1.1.0 → 1.2.0** — 8-row table (package name, SDK directory, CLI bridge, gsd_run launcher, observability, command-routing-hub, bin/lib modules, hooks structure) with columns: Change | 1.1.0 state | 1.2.0 state | Impact on pi-gsd-redux. Source the table from RESEARCH.md §Structural Changes.

  3. **gsd-tools CLI Surface Catalog** — Per D-03 (code-level inspection). Document the 8 command families with representative sub-commands from each family:
     - Atomic Commands (20+): `state load`, `state json`, `state update`, `resolve-model`, `find-phase`, `commit`, `history-digest`, `summary-extract`, `websearch`
     - Phase Operations (5): `phase next-decimal`, `phase add`, `phase insert`, `phase remove`, `phase complete`
     - Roadmap Operations (3): `roadmap get-phase`, `roadmap analyze`, `roadmap update-plan-progress`
     - Requirements Operations (1): `requirements mark-complete`
     - Milestone Operations (1): `milestone complete`
     - Validation (3): `validate consistency`, `validate health`, `validate agents`
     - Progress (1): `progress` with json/table/bar output modes
     - UAT/Audit/Intel (4+): `audit-uat`, `uat render-checkpoint`, `audit-open`, `intel query/status/update`
     Note: mark as VERIFIED against the actual `gsd-tools.cjs` source extracted from the 1.2.0 tarball.

  4. **gsd_run Launcher Convention** — Per D-03. Document:
     - `gsd_run` is NOT a binary — it's a shell function defined in `_runtime-launcher.snippet.sh`
     - Function body: `gsd_run() { node "$GSD_TOOLS" "$@"; }`
     - `GSD_TOOLS` resolved across 4 fallback paths (project `get-shit-done/bin/`, `.claude/get-shit-done/bin/`, `PATH`, npm package `get-shit-done/bin/`)
     - **Pi runtime implication:** Pi has no shell execution environment — cannot source `.sh` snippets or run `gsd_run`. Phase 8 must use `require.resolve('@opengsd/gsd-core/get-shit-done/bin/gsd-tools.cjs')` for path resolution and `node <resolvedPath>` for invocation.
     - Cross-reference Common Pitfall #4 (RESEARCH.md): do not hardcode paths.

  5. **DispatchLogger Seam Assessment** — Per D-03, UPSTREAM-04. Document:
     - **API surface:** `DispatchLogger` interface with `onEvent(event: DispatchEvent): void`
     - **Event shape:** `{ traceId (UUID v4), parentTraceId?, command, args? (only when includeArgs=true AND GSD_AUDIT_ARGS=1), result: { kind, ...payload }, timestamp (ISO 8601) }`
     - **Default behavior** (`createDefaultLogger`): silent on ok, structured JSON to stderr on error, opt-in audit file at `.planning/.gsd-trace.jsonl` when `GSD_AUDIT=1` or `config.audit.enabled=true`, args redacted by default
     - **Wiring point:** `command-routing-hub.cjs` accepts optional `logger: { onEvent }` — calls `logger.onEvent(event)` after every dispatch. NoOp by default.
     - **Suitability for v2.0 Auto Orchestration (UPSTREAM-04):** ✅ Provides structured `traceId`-based dispatch tracing; ✅ opt-in audit file is crash-safe (sync `fs.appendFileSync`); ✅ error events emit to stderr; ⚠️ Limited to Hub dispatch events only — does not cover orchestration lifecycle transitions; ⚠️ Args redacted by default. **Recommendation:** Use DispatchLogger for Hub-level dispatch tracing. Add a separate `OrchestrationLogger` for orchestration lifecycle events (Unit dispatch decisions, gate outcomes, state transitions), following the same `{ onEvent }` pattern and writing to the same `.gsd-trace.jsonl` with a different event type discriminator.

  6. **State of the Art Comparison** — 4-row table from RESEARCH.md §State of the Art: Old Approach (v1.0) | Current Approach (v1.2.0) | Impact. Cover: gsd_query bridge → gsd-tools CLI + gsd_run, AUTO_MODE_CHECKLIST → native TS orchestration, package name → @opengsd/gsd-core, no event tracing → DispatchLogger seam.

  7. **Phase 8 Migration Implications** — Concrete checklist for Phase 8 planner:
     - `src/official.ts:5`: `OFFICIAL_PACKAGE_NAME` → `@opengsd/gsd-core`
     - `package.json`: dependency → `@opengsd/gsd-core@1.2.0`
     - `src/gsd-query-tool.ts`: SDK imports broken — must delete or rewire to `gsd-tools.cjs` / `gsd_run()`
     - `src/prompt-transform.ts`: `$GSD_SDK` transform → redirect from `gsd_query` Pi tool to `gsd_run` shell function or direct `node gsd-tools.cjs` invocation
     - All path-rewrite inputs, comments, test fixtures: rename `@opengsd/get-shit-done-redux` → `@opengsd/gsd-core`
     - `npm run check` must pass (typecheck + test + build + doctor)

  8. **Common Pitfalls for Phase 8** — 3 pitfalls from RESEARCH.md:
     - Assuming `$ARGUMENTS` is env var or tool input
     - Treating gsd-tools.cjs as SDK replacement (it's a transition bridge, not target architecture)
     - Skipping `_runtime-launcher.snippet.sh` resolution logic (hardcoding paths fails across install locations)

  Per D-04: This is a surface catalog for code-level inspection, NOT a full v1.1.0→v1.2.0 diff. Phase 8 performs the actual diff and migration.
  </action>
  <acceptance_criteria>
  <behavior>The gsd-tools CLI surface table covers 8 command families with representative sub-commands, verified against actual `gsd-tools.cjs` source extracted from the 1.2.0 tarball.</behavior>
  <behavior>The gsd_run launcher section documents it as a shell function (not binary), the 4 fallback paths, and the Pi runtime implication (no shell environment).</behavior>
  <behavior>The DispatchLogger assessment covers event shape (traceId, command, result, timestamp), wiring point (command-routing-hub.cjs logger injection), audit format (JSONL to .gsd-trace.jsonl), default redaction policy, and UPSTREAM-04 suitability verdict with recommendation.</behavior>
  <behavior>The Phase 8 migration implications section lists 5 concrete code locations (`src/official.ts:5`, `package.json`, `src/gsd-query-tool.ts`, `src/prompt-transform.ts`, all path-rewrite inputs) with specific migration actions.</behavior>
  <behavior>The artifact does NOT include a full v1.1.0→v1.2.0 diff (per D-04 — that's Phase 8's job).</behavior>
  </acceptance_criteria>
  <verify>
    <automated>Read `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md` — confirm gsd-tools CLI surface catalog ≥8 families, gsd_run shell function documented with Pi implication, DispatchLogger seam assessment with UPSTREAM-04 verdict, Phase 8 migration checklist covers 5 code locations, no full v1.1.0→v1.2.0 diff (per D-04).</automated>
  </verify>
  <done>upstream-1.2.0-impact.md exists at `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/upstream-1.2.0-impact.md` with ≥5 sections, gsd-tools CLI catalog (per D-03), gsd_run convention, DispatchLogger assessment (UPSTREAM-04), Phase 8 migration checklist, no full diff (per D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi slash command → template content | User-supplied argv reaches prompt content via `$ARGUMENTS` substitution — treated as untrusted LLM input |
| Upstream 1.2.0 source → spike artifact | Cataloged API surface must faithfully represent actual source; misrepresentation would feed incorrect surface into Phase 8 planning |
| gsd-pi fork → module map | Mapping decisions (mirror/defer/N/A) feed v2.0 module design; incorrect mapping could cause Phase 9-12 rework |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | pi-argv.md — $ARGUMENTS substitution | accept | Pi's `parseCommandArgs()` handles input sanitization via bash-style quoting. Research artifact is documentation, not executable code — no injection surface. |
| T-07-02 | Tampering | upstream-1.2.0-impact.md — gsd-tools command catalog | mitigate | Verify each command family claim against actual `gsd-tools.cjs` source extracted from `npm pack @opengsd/gsd-core@1.2.0`. Task 3 action explicitly requires this verification. |
| T-07-03 | Information Disclosure | upstream-1.2.0-impact.md — DispatchLogger audit format | accept | Documenting the audit format (`.gsd-trace.jsonl` JSONL structure) does not expose secrets — the audit file is project-scoped. Default redaction policy (args hidden unless `GSD_AUDIT_ARGS=1`) is documented. |
| T-07-SC | Tampering | npm pack @opengsd/gsd-core@1.2.0 | accept | `npm pack` downloads and extracts a read-only tarball; no scripts executed. The package is from the npm registry under the same org (`@opengsd`) as the current dependency. No checkpoint gate needed for this phase since the tarball is inspected, not installed. |
</threat_model>

<verification>
All three spike artifacts exist at `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/spike/`. Each artifact is self-consistent and all source claims are reproducible by re-running the inspection commands documented within each artifact.

Cross-artifact consistency checks:
- pi-argv.md does NOT contain gsd-tools CLI or gsd_run content (per D-06 scope boundary)
- upstream-1.2.0-impact.md does NOT contain full v1.1.0→v1.2.0 diff (per D-04)
- gsd-pi-module-map.md does NOT reference `extensions/gsd/{auto,state-reconciliation,safety}/` (per D-08)
- All 5 modules in gsd-pi-module-map.md are marked Mirror (per D-09)
- upstream-1.2.0-impact.md Phase 8 migration checklist covers all 5 code locations from RESEARCH.md
</verification>

<success_criteria>
- [ ] `spike/pi-argv.md` documents the verified Pi argv-passing mechanism with all 6 substitution patterns and a concrete reproducer
- [ ] `spike/gsd-pi-module-map.md` maps all 5 gsd-pi modules to v2.0 surfaces with Mirror decisions (per D-09), ADR references, and correct file paths (per D-08)
- [ ] `spike/upstream-1.2.0-impact.md` catalogs 1.2.0 gsd-tools CLI surface (per D-03), gsd_run launcher, and DispatchLogger seam with UPSTREAM-04 suitability assessment
- [ ] Phase 8 planner can proceed with known 1.2.0 surface — no re-investigation needed
- [ ] Phase 9-12 planners have verified Pi runtime contract and gsd-pi module blueprints
- [ ] All three artifacts committed to git
- [ ] ROADMAP.md updated with plan list
</success_criteria>

<output>
Create `.planning/phases/07-pi-runtime-spike-gsd-pi-module-mapping/07-01-SUMMARY.md` when done
</output>
