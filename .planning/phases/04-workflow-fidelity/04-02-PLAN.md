---
phase: 04-workflow-fidelity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/doctor.ts
  - tests/doctor.test.ts
  - package.json
autonomous: true
requirements:
  - D-02
  - D-05

must_haves:
  truths:
    - "Doctor command verifies @juicesharp/rpiv-ask-user-question is installed and exports a compatible ask_user_question tool"
    - "Doctor command warns when rpiv is missing, indicating workflows that use AskUserQuestion will fall back to --text mode"
    - "Doctor check is additive — existing checks for official package, pi-subagents, and generated drift all still pass"
  artifacts:
    - path: "src/doctor.ts"
      provides: "rpiv-ask-user-question availability check"
      contains: "rpiv"
    - path: "tests/doctor.test.ts"
      provides: "Unit tests for rpiv doctor check"
      min_lines: 20
  key_links:
    - from: "src/doctor.ts"
      to: "@juicesharp/rpiv-ask-user-question"
      via: "Node module resolution (createRequire or require.resolve)"
      pattern: "rpiv-ask-user-question"
---

<objective>
Add rpiv-ask-user-question availability check to the doctor command and add it as a peer dependency recommendation in package.json.

Purpose: Ensure users know when the ask_user_question tool is missing, preventing silent workflow failures.
Output: Doctor check for rpiv, tests, and package.json peer dependency.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-workflow-fidelity/04-CONTEXT.md
@src/doctor.ts
@tests/doctor.test.ts
@package.json
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add rpiv-ask-user-question availability check to doctor</name>
  <files>src/doctor.ts, tests/doctor.test.ts</files>
  <behavior>
    - Test 1: Doctor reports ok when rpiv-ask-user-question is installed and exports ask_user_question tool
    - Test 2: Doctor reports warning when rpiv-ask-user-question is not installed
    - Test 3: Doctor check does not crash if rpiv module throws on require (malformed package)
    - Test 4: Existing doctor checks (official package, pi-subagents, generated drift) still pass independently
  </behavior>
  <action>Add a rpiv-ask-user-question availability check to `runDoctor` in src/doctor.ts.

The check should:
1. Use Node module resolution (similar to existing `resolveOfficialPackage` and `resolvePiSubagentsPackage` patterns) to find `@juicesharp/rpiv-ask-user-question`
2. If found, verify it exports a default function (the extension entry point per D-04 research)
3. If not found, add a warning-level message: `@juicesharp/rpiv-ask-user-question is not installed. AskUserQuestion-dependent workflows (discuss, plan, etc.) will use --text fallback mode. Install with: pi install npm:@juicesharp/rpiv-ask-user-question`
4. Follow the existing doctor message pattern: `{ ok: boolean, messages: string[] }`

The rpiv check should run after pi-subagents check but before the generated-drift check, as it's at the same dependency level as pi-subagents.

**package.json**: Add `@juicesharp/rpiv-ask-user-question` as a `peerDependency` (not a regular dependency) with version `^1.15.0`. This signals that it's needed at runtime but shouldn't be auto-installed by npm (it's installed via `pi install`).

After implementing, run: `npm test -- --reporter=verbose tests/doctor.test.ts`
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose tests/doctor.test.ts</automated>
  </verify>
  <done>rpiv availability check added to doctor. Warning when missing, ok when present. All existing doctor tests still pass. package.json updated with peerDependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doctor check → npm resolution | Module resolution could fail or resolve wrong path on monorepos or workspaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation | doctor | accept | Doctor check is informational only — missing rpiv produces a warning, not an error. No security boundary crossed. |
| T-04-SC | Tampering | npm installs | mitigate | rpiv is a peer dependency, not auto-installed. User must explicitly install via pi install. No new build dependencies added. |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm test` passes (all tests including new doctor tests)
- `npm run check` passes (typecheck + test + doctor)
- `node dist/cli.js doctor --prompts generated/prompts --agents --cwd .` shows rpiv check result
</verification>

<success_criteria>
- Doctor command shows a clear message when @juicesharp/rpiv-ask-user-question is missing
- Doctor command shows ok when it's installed
- package.json lists rpiv as a peerDependency
- All existing doctor functionality unaffected
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/04-workflow-fidelity/04-02-SUMMARY.md` when done
</output>