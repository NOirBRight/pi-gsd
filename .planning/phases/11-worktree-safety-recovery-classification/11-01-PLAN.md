---
phase: 11-worktree-safety-recovery-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/recovery/types.ts
  - src/recovery/classify-failure.ts
  - src/recovery/index.ts
  - src/orchestrator/types.ts
  - src/orchestrator/reconciliation.ts
  - src/orchestrator/state-machine.ts
  - src/index.ts
  - tests/recovery.test.ts
  - tests/orchestrator.test.ts
autonomous: true
requirements:
  - RECOV-01
  - RECOV-02
must_haves:
  truths:
    - "classifyFailure(input) returns exactly one of the eight locked classes from D-09 and never returns other, unknown, or null per D-12."
    - "Every recovery class maps to exactly one locked action per D-13 and D-14."
    - "Phase 10 ReconciliationBlocker inputs map by explicit reasonCode table per D-10, and partial-write stops while preserving written[] evidence per D-11."
    - "GateResult failures retain reason, resumeHint, and evidence while embedding recoveryDecision per D-16."
    - "Telemetry exitReason uses the recovery class taxonomy per D-17."
  artifacts:
    - path: src/recovery/types.ts
      provides: "RecoveryClass, RecoveryAction, RecoveryDecision, classifier input/evidence types, RECOVERY_CLASSES, RECOVERY_ACTIONS"
    - path: src/recovery/classify-failure.ts
      provides: "classifyFailure(input) and explicit reason-code/action tables"
    - path: src/orchestrator/types.ts
      provides: "GateResult and OrchestrationEvent recoveryDecision/exitReason fields"
    - path: src/orchestrator/reconciliation.ts
      provides: "ReconciliationFailedError to typed recoveryDecision gate failure adapter"
    - path: src/orchestrator/state-machine.ts
      provides: "recovery action propagation into retry/pause/stop events"
  key_links:
    - from: src/orchestrator/reconciliation.ts
      to: src/recovery/classify-failure.ts
      via: "classifyFailure({ kind: reconciliation, reasonCode, blockers, written, evidence })"
      pattern: "classifyFailure"
    - from: src/orchestrator/state-machine.ts
      to: src/orchestrator/types.ts
      via: "GateResult.recoveryDecision drives retryable/action and event exitReason"
      pattern: "recoveryDecision"
---

<objective>
Implement the Phase 11 recovery taxonomy as the shared typed decision layer for orchestration gates, reconciliation failures, and telemetry.

Purpose: RECOV-01 and RECOV-02 require a fail-closed, explicit recovery classifier before worktree safety can return typed decisions. This plan implements D-09 through D-17 for recovery classification, action mapping, GateResult handoff, and telemetry exit reasons. D-18 bounded persistence is wired in Plan 11-03.
Output: `src/recovery/` module, orchestrator type/event integration, tests proving all eight classes and action mappings.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
@.planning/phases/11-worktree-safety-recovery-classification/11-RESEARCH.md
@.planning/phases/11-worktree-safety-recovery-classification/11-PATTERNS.md
@.planning/phases/11-worktree-safety-recovery-classification/11-VALIDATION.md
@src/state-reconciliation/types.ts
@src/state-reconciliation/errors.ts
@src/state-reconciliation/catalog.ts
@src/orchestrator/types.ts
@src/orchestrator/reconciliation.ts
@src/orchestrator/state-machine.ts
@src/index.ts
@tests/state-reconciliation.test.ts
@tests/orchestrator.test.ts
</context>

## Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Typed failure taxonomy for recovery decisions | Covered by Tasks 1-2 |
| REQ | RECOV-01: classifyFailure returns one of 8 explicit classes, no other | Covered by Task 1 |
| REQ | RECOV-02: each class maps to exactly one action and telemetry exit reasons use taxonomy | Covered by Tasks 1-2 |
| RESEARCH | No new packages; use TypeScript tables, Vitest, NodeNext imports | Covered by Tasks 1-2 |
| RESEARCH | Phase 10 blockers map by reasonCode, not prose | Covered by Tasks 1-2 |
| CONTEXT | D-09, D-10, D-11, D-12 | Covered by Task 1 |
| CONTEXT | D-13, D-14, D-15 | Covered by Task 1 tests and classifier fixtures |
| CONTEXT | D-16, D-17 | Covered by Task 2 |
| CONTEXT | D-18 | Covered by Plan 11-03 |
| CONTEXT deferred | Tool Contract Bridge, Settings Bridge, full worktree lifecycle, parallel slice orchestration | Excluded as deferred ideas |

## Artifacts this phase produces

- New file path: `src/recovery/types.ts`
- New file path: `src/recovery/classify-failure.ts`
- New file path: `src/recovery/index.ts`
- New file path: `tests/recovery.test.ts`
- New symbols: `RECOVERY_CLASSES`, `RECOVERY_ACTIONS`, `RecoveryClass`, `RecoveryAction`, `RecoveryDecision`, `RecoveryDecisionEvidence`, `RecoveryFailureInput`, `RecoveryFailureKind`, `ReconciliationRecoveryInput`, `GateRecoveryInput`, `DispatchRecoveryInput`, `ArtifactGateRecoveryInput`, `WorktreeRecoveryInput`, `classifyFailure`
- Modified symbols/fields: `GateResult.recoveryDecision`, `OrchestrationEvent.recoveryDecision`, `OrchestrationEvent.exitReason`, `StopReason` compatibility with recovery classes, `toGateFailure`, `handleGateFailure`
- New public exports: `export * from "./recovery/index.js"` from `src/index.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and table-test the fixed recovery taxonomy</name>
  <files>src/recovery/types.ts, src/recovery/classify-failure.ts, src/recovery/index.ts, src/index.ts, tests/recovery.test.ts</files>
  <read_first>
    @.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
    @.planning/phases/11-worktree-safety-recovery-classification/11-RESEARCH.md
    @.planning/phases/11-worktree-safety-recovery-classification/11-PATTERNS.md
    @src/state-reconciliation/types.ts
    @src/state-reconciliation/errors.ts
    @src/state-reconciliation/catalog.ts
    @src/index.ts
    @tests/state-reconciliation.test.ts
  </read_first>
  <behavior>
    - `RECOVERY_CLASSES` equals exactly the eight D-09 values in order: transient-external-failure, repairable-state-drift, unrepaired-state-drift, worktree-invalid, dispatch-contract-invalid, artifact-gate-failed, user-input-required, internal-invariant-violation.
    - `RECOVERY_ACTIONS` maps exactly as D-14 locks, with one action for every class and no configurable override per D-13.
    - No exported class, fixture, or decision uses `other`, `unknown`, or `null` as a recovery class per D-12.
    - Every `ReconciliationReasonCode` maps through an explicit table per D-10; `partial-write` maps to class `internal-invariant-violation`, action `stop`, and preserves `written[]` evidence per D-11.
    - v1.0 triage families from RESEARCH map to explicit classes: provider/network transient, missing auth, dispatch contract mismatch, artifact gate failure, state drift, partial writes, invalid worktree, and internal impossible state per D-15.
  </behavior>
  <action>Create `src/recovery/types.ts` with `RECOVERY_CLASSES`, `RecoveryClass`, `RecoveryAction`, `RECOVERY_ACTIONS`, `RecoveryDecision`, bounded evidence types, and discriminated `RecoveryFailureInput` variants. Create `src/recovery/classify-failure.ts` with `classifyFailure(input)` and explicit tables for class-to-action and Phase 10 `ReconciliationReasonCode`-to-class mapping; do not inspect blocker prose or rely on `suggestedNextAction` for classification. Ensure unmodeled structured inputs classify to a stop-class such as `internal-invariant-violation` or `dispatch-contract-invalid`, never to `other` or `unknown`, per D-12. Create `src/recovery/index.ts` and export it from `src/index.ts` using NodeNext `.js` suffixes. Write `tests/recovery.test.ts` before production code and keep tests table-driven with `satisfies` patterns from `tests/state-reconciliation.test.ts`; follow 11-PATTERNS analogs `src/state-reconciliation/types.ts` for enum-array/types, `src/state-reconciliation/catalog.ts` for table-driven transforms, `src/state-reconciliation/errors.ts` for structured reconciliation handoff, `src/state-reconciliation/index.ts` for barrel exports, and `D:/Workstation/gsd-pi-fork/src/resources/extensions/gsd/recovery-classification.ts` only for exhaustive classifier shape, not taxonomy names.</action>
  <acceptance_criteria>
    - Source assertion: `grep -R "other\|unknown" src/recovery tests/recovery.test.ts` finds no recovery class fallback; occurrences of Phase 10 `unknown-drift` are allowed only as a reconciliation reason code input.
    - Behavior assertion: all eight classes and exactly four actions are covered by tests, with no missing class in `RECOVERY_ACTIONS`.
    - Behavior assertion: `partial-write` decision includes `written[]` evidence and action `stop`.
    - Public API assertion: `src/index.ts` exports `./recovery/index.js`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/recovery.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`classifyFailure(input)` satisfies RECOV-01 and the class/action table satisfies RECOV-02 for non-orchestrator inputs, with D-09 through D-15 covered by table-driven tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed typed recovery decisions in gate failures and telemetry events</name>
  <files>src/orchestrator/types.ts, src/orchestrator/reconciliation.ts, src/orchestrator/state-machine.ts, tests/orchestrator.test.ts</files>
  <read_first>
    @.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
    @.planning/phases/11-worktree-safety-recovery-classification/11-PATTERNS.md
    @src/recovery/types.ts
    @src/recovery/classify-failure.ts
    @src/orchestrator/types.ts
    @src/orchestrator/reconciliation.ts
    @src/orchestrator/state-machine.ts
    @tests/orchestrator.test.ts
  </read_first>
  <behavior>
    - `GateResult` failure branch includes optional `recoveryDecision` while retaining `reason`, `retryable`, `resumeHint`, and `evidence` per D-16.
    - `OrchestrationEvent` includes `recoveryDecision?: RecoveryDecision` and `exitReason?: RecoveryClass`; stop/pause/retry events set `exitReason` to `recoveryDecision.class` when present per D-17.
    - `toGateFailure(ReconciliationFailedError)` calls `classifyFailure` with `reasonCode`, blockers, written, and evidence so Phase 10 blockers are classified by typed reason code per D-10.
    - `handleGateFailure` maps `recoveryDecision.action === "retry"` to retry scheduling and uses stop-class decisions to stop rather than silently pause when action is `stop`; compatibility with existing retry budget tests is preserved.
  </behavior>
  <action>Extend `src/orchestrator/types.ts` to import recovery types and add `recoveryDecision` and `exitReason` fields without removing legacy fields. Update `src/orchestrator/reconciliation.ts` so reconciliation blockers produce `recoveryDecision` via `classifyFailure` and set `reason` to the recovery class while keeping bounded reason-code evidence. Update `src/orchestrator/state-machine.ts` so gate failure events carry `recoveryDecision` and `exitReason`, and action semantics are exact: `retry` schedules retry subject to existing node repair budget, `pause-with-remediation` pauses, `self-heal` records the self-heal decision through existing Phase 10 apply-mode behavior, and `stop` stops the snapshot. Add tests to `tests/orchestrator.test.ts` for reconciliation gate failure classification, action-to-status behavior, retry event exit reasons, and existing gate order preservation per D-16 and D-17. Follow 11-PATTERNS analogs: existing `src/orchestrator/types.ts` for GateResult/OrchestrationEvent extension, `src/orchestrator/reconciliation.ts` for bounded blocker evidence, `src/orchestrator/state-machine.ts` for retry/pause event propagation, and existing `tests/orchestrator.test.ts` for gate-order and retry-budget fixture style.</action>
  <acceptance_criteria>
    - Source assertion: all new local imports use `.js` suffixes.
    - Behavior assertion: a `partial-write` reconciliation failure produces `recoveryDecision.class === "internal-invariant-violation"`, `action === "stop"`, and an event `exitReason === "internal-invariant-violation"`.
    - Behavior assertion: a transient external classifier fixture with action `retry` still respects `workflow.node_repair_budget`.
    - Compatibility assertion: legacy fields `reason`, `resumeHint`, and `evidence` remain present on failure `GateResult` objects.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/recovery.test.ts tests/orchestrator.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Gate failures and orchestration events expose typed recovery decisions and recovery-class exit reasons while preserving existing compatibility fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate failure input → recovery classifier | Structured and unstructured runtime failures cross into decision logic that controls retry/pause/stop. |
| Reconciliation report → orchestrator gate | Phase 10 blockers and partial-write evidence cross into dispatch safety decisions. |
| Recovery decision → telemetry event | Recovery decisions cross from classifier output into orchestrator lifecycle events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `classifyFailure` | mitigate | Exact D-09/D-14 tables and no fallback class prevent unsafe continuation from unclassified failures. |
| T-11-02 | Repudiation | `ReconciliationFailedError` mapping | mitigate | Preserve reasonCode, written[], and bounded evidence in RecoveryDecision so partial writes cannot be hidden. |
| T-11-03 | Denial of Service | retry handling | mitigate | Keep existing `workflow.node_repair_budget` as the retry bound; only `retry` action schedules retry. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; if executor adds one, stop and require package legitimacy review. |
</threat_model>

<verification>
Run targeted tests after each task, then run `npm run typecheck`. At plan close run `npx vitest run tests/recovery.test.ts tests/orchestrator.test.ts` and `npm run check` if targeted checks are green.
</verification>

<success_criteria>
- `src/recovery/classify-failure.ts` exports `classifyFailure(input)` satisfying RECOV-01 with exactly eight classes and no fallback class.
- `RECOVERY_ACTIONS` satisfies RECOV-02 with exactly one action for each class.
- Phase 10 reason codes map by explicit table, and `partial-write` stops while preserving written[] evidence.
- Gate failures include `recoveryDecision`; telemetry `exitReason` uses the recovery class taxonomy.
</success_criteria>

<output>
Create `.planning/phases/11-worktree-safety-recovery-classification/11-01-SUMMARY.md` when done.
</output>
