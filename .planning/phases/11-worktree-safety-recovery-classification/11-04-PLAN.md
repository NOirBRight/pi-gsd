---
phase: 11-worktree-safety-recovery-classification
plan: 04
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - src/worktree-safety/types.ts
  - src/worktree-safety/git.ts
  - src/worktree-safety/lease.ts
  - src/worktree-safety/prepare-unit-root.ts
  - src/orchestrator/gates.ts
  - src/orchestrator/types.ts
  - src/orchestrator/state-machine.ts
  - tests/worktree-safety.test.ts
  - tests/orchestrator.test.ts
  - tests/orchestrator-journal.test.ts
autonomous: true
requirements:
  - WTREE-02
  - RECOV-02
gap_closure: true
must_haves:
  truths:
    - "A real lease release operation emits lease_released after source-writing Unit completion or cleanup per D-08."
    - "lease_released flows through GateResult/AdvanceResult.events to JournalAdapter.append, using the same runtime journal path as lease_acquired and lease_stale_reclaimed per D-08 and D-18."
    - "Integration tests prove lease_released is persisted by an actual orchestrator lifecycle flow, not by manually appending a synthetic event."
  artifacts:
    - path: src/worktree-safety/lease.ts
      provides: "owned lease release operation that emits lease_released and safely removes or marks the owned lease record"
    - path: src/worktree-safety/prepare-unit-root.ts
      provides: "release helper input construction matching prepareUnitRoot ownership/session evidence"
    - path: src/orchestrator/state-machine.ts
      provides: "post-dispatch/cleanup release hook that forwards release journal events through AdvanceResult.events"
    - path: tests/orchestrator-journal.test.ts
      provides: "integration coverage for persisted lease_released from actual createAutoOrchestrator flow"
  key_links:
    - from: src/orchestrator/state-machine.ts
      to: src/worktree-safety/lease.ts
      via: "source-writing Unit completion calls real release operation"
      pattern: "release.*Lease|lease_released"
    - from: src/orchestrator/state-machine.ts
      to: src/orchestrator/index.ts
      via: "AdvanceResult.events consumed by createAutoOrchestrator record()"
      pattern: "events:.*lease"
    - from: src/orchestrator/index.ts
      to: src/orchestrator/journal.ts
      via: "JournalAdapter.append persists each runtime event"
      pattern: "journal\.append"
---

<objective>
Close the Phase 11 verification gap by adding the missing real lease release lifecycle path.

Purpose: D-08 requires lease acquisition, release, and stale-reclaim evidence to be recorded in the orchestrator journal. Verification proved acquire and stale-reclaim already flow through runtime, but release exists only as a builder/manual append path.
Output: a real owned lease release operation, an orchestrator completion/cleanup hook that emits `lease_released`, and integration tests proving journal persistence without manual synthetic appends.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
@.planning/phases/11-worktree-safety-recovery-classification/11-RESEARCH.md
@.planning/phases/11-worktree-safety-recovery-classification/11-PATTERNS.md
@.planning/phases/11-worktree-safety-recovery-classification/11-VERIFICATION.md
@.planning/phases/11-worktree-safety-recovery-classification/11-02-SUMMARY.md
@.planning/phases/11-worktree-safety-recovery-classification/11-03-SUMMARY.md
@src/worktree-safety/types.ts
@src/worktree-safety/git.ts
@src/worktree-safety/lease.ts
@src/worktree-safety/prepare-unit-root.ts
@src/orchestrator/gates.ts
@src/orchestrator/types.ts
@src/orchestrator/state-machine.ts
@src/orchestrator/index.ts
@src/orchestrator/journal.ts
@tests/worktree-safety.test.ts
@tests/orchestrator.test.ts
@tests/orchestrator-journal.test.ts
</context>

## Source Coverage Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Fail-closed worktree validation includes complete lease evidence lifecycle | Covered by Tasks 1-2 |
| REQ | WTREE-02: lease ownership validation includes release lifecycle evidence | Covered by Tasks 1-2 |
| REQ | RECOV-02: journal/telemetry uses recovery taxonomy and bounded event evidence | Covered by Task 2 |
| RESEARCH | Lease evidence remains journal-readable durable facts | Covered by Tasks 1-2 |
| CONTEXT | D-05: lease ownership bound to unitId + session identity + phase/branch/process/host evidence | Covered by Task 1 |
| CONTEXT | D-08: acquisition, release, stale-reclaim evidence recorded in orchestrator journal | Covered by Tasks 1-2 |
| CONTEXT | D-18: bounded evidence only; no raw user text/secrets/unbounded errors | Covered by Task 2 |
| VERIFICATION | Missing real lease release operation/lifecycle hook | Covered by Task 1 |
| VERIFICATION | Missing GateResult/AdvanceResult.events forwarding to JournalAdapter.append | Covered by Task 2 |
| VERIFICATION | Missing integration coverage for actual runtime persistence | Covered by Task 2 |
| CONTEXT deferred | Full worktree lifecycle projection, Tool Contract Bridge, Settings Bridge, parallel slice orchestration | Excluded as deferred ideas |

## Artifacts this phase produces

- `releaseLeaseOwnership` or equivalent exported operation in `src/worktree-safety/lease.ts` that verifies current ownership and emits a bounded `lease_released` event.
- Worktree safety dependency/type updates needed to remove or mark the owned lease record without unsafe filesystem writes outside `.planning`.
- Orchestrator post-dispatch/cleanup release hook for source-writing Units that forwards `lease_released` via `AdvanceResult.events` into the existing `createAutoOrchestrator` `JournalAdapter.append` loop.
- Tests proving persisted `lease_released` comes from real lifecycle execution, not `appendJournalEvent({ event: leaseReleasedEvent(...) })`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add owned lease release operation and unit tests</name>
  <files>src/worktree-safety/types.ts, src/worktree-safety/git.ts, src/worktree-safety/lease.ts, src/worktree-safety/prepare-unit-root.ts, tests/worktree-safety.test.ts</files>
  <read_first>
    @.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
    @.planning/phases/11-worktree-safety-recovery-classification/11-VERIFICATION.md
    @src/worktree-safety/types.ts
    @src/worktree-safety/git.ts
    @src/worktree-safety/lease.ts
    @src/worktree-safety/prepare-unit-root.ts
    @tests/worktree-safety.test.ts
  </read_first>
  <behavior>
    - Releasing a lease held by the current `unitId` + `sessionId` + `phase` + `branch` + `root` + host/process evidence succeeds and returns a `lease_released` journal event per D-05 and D-08.
    - Releasing a missing, wrong-owner, incomplete, or contradictory lease fails closed with a typed recovery decision and does not emit a false `lease_released` event.
    - Release path resolution keeps lease files under `.planning` and refuses paths outside `.planning` per existing lease path safety.
    - Release evidence is bounded to D-18 fields and uses the existing `leaseReleasedEvent` builder from a real operation rather than direct test/manual append.
  </behavior>
  <action>Add a production release operation in `src/worktree-safety/lease.ts`, named `releaseLeaseOwnership` or another explicit equivalent, that accepts the same ownership/session/root/branch inputs used by `checkLeaseOwnership`. It must resolve the lease path with the existing `.planning` guard, read the current record, compare ownership with the same owner semantics as acquisition, then remove the lease file or mark it released only when the current worker owns it. If removal is used, add `unlinkSync` or a narrowly named optional delete method to `WorktreeSafetyDeps` and `defaultWorktreeSafetyDeps`; if marker-write is used, keep the lease record under `.planning` and ensure a later acquire can proceed deterministically. Return a structured Result carrying `journalEvents: [leaseReleasedEvent(...)]` on success and a `RecoveryDecision` on failure. Add a small helper in `prepare-unit-root.ts` only if needed to build the same `PrepareUnitRootInput` shape for release; do not broaden into full worktree lifecycle management. Write tests before implementation in `tests/worktree-safety.test.ts` for owned release, wrong-owner no-release, missing lease no synthetic release, and outside-.planning lease path refusal.</action>
  <acceptance_criteria>
    - `src/worktree-safety/lease.ts` contains a real release function that calls `leaseReleasedEvent` from production code.
    - Owned release returns exactly one `lease_released` journal event with `unitId`, `phase`, `root`, `branch`, `attempt`, `action`, `recoveryClass`, and `reasonCode`.
    - Wrong-owner/missing/unsafe-path release cases do not emit `lease_released` and return typed failure decisions.
    - Tests do not manually append `leaseReleasedEvent` to the orchestrator journal to claim lifecycle coverage.
  </acceptance_criteria>
  <done>Owned lease release is implemented as a real worktree-safety operation and covered by unit tests for success and fail-closed cases.</done>
  <verify>
    <automated>npx vitest run tests/worktree-safety.test.ts tests/recovery.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire release through actual orchestrator lifecycle and journal persistence</name>
  <files>src/orchestrator/gates.ts, src/orchestrator/types.ts, src/orchestrator/state-machine.ts, tests/orchestrator.test.ts, tests/orchestrator-journal.test.ts</files>
  <read_first>
    @.planning/phases/11-worktree-safety-recovery-classification/11-VERIFICATION.md
    @.planning/phases/11-worktree-safety-recovery-classification/11-03-SUMMARY.md
    @src/orchestrator/gates.ts
    @src/orchestrator/types.ts
    @src/orchestrator/state-machine.ts
    @src/orchestrator/index.ts
    @src/orchestrator/journal.ts
    @src/worktree-safety/lease.ts
    @tests/orchestrator.test.ts
    @tests/orchestrator-journal.test.ts
  </read_first>
  <behavior>
    - After a source-writing Unit completes successfully, the orchestrator invokes the real release operation and includes `lease_released` in `AdvanceResult.events`.
    - If dispatch or post-dispatch cleanup fails after acquisition, cleanup still attempts release for the owned lease and forwards any safe release event through `AdvanceResult.events`.
    - `createAutoOrchestrator(...journal...)` persists the release event through its existing `record()` loop and `JournalAdapter.append`, the same path used by acquire/stale-reclaim.
    - Integration coverage reads `.planning/orchestration-state.json` and proves `lease_released` was produced by actual `start()`/`advance()` lifecycle execution, not direct `appendJournalEvent` or direct `leaseReleasedEvent` append.
  </behavior>
  <action>Wire the release operation into the existing orchestrator runtime path. Prefer a small helper near `prepareUnitRoot` in `src/orchestrator/gates.ts` or a narrow release hook in `src/orchestrator/state-machine.ts` that builds the same worktree-safety input from `snapshot` and `unit`, runs only for source-writing Units that required lease ownership, and returns typed `journalEvents[]`. In `advanceOrchestration`, call this hook after successful dispatch/post-dispatch completion and also in cleanup paths where dispatch or post-dispatch gate failure occurs after a lease was acquired. Append the resulting `lease_released` event to `AdvanceResult.events` so `src/orchestrator/index.ts` persists it through the existing `record()` loop; do not add a second journal writer. Preserve current acquire/stale-reclaim event behavior. Add tests in `tests/orchestrator.test.ts` for event forwarding and in `tests/orchestrator-journal.test.ts` using `createAutoOrchestrator` plus a temp `.planning` journal to assert persisted event order includes acquire then release for successful execute flow. The release test must fail if it manually calls `appendJournalEvent` with `leaseReleasedEvent` instead of running `start()`/`advance()`.</action>
  <acceptance_criteria>
    - Successful source-writing execute flow produces `lease_released` in `AdvanceResult.events`.
    - Actual `createAutoOrchestrator` flow persists `lease_released` in `.planning/orchestration-state.json` via `JournalAdapter.append`.
    - The integration test proves release persistence without manually appending a synthetic release event.
    - Acquire and stale-reclaim tests remain green and still use the same event forwarding path.
    - Release event evidence remains bounded/redacted and contains no raw env/user text/secrets.
  </acceptance_criteria>
  <done>Actual orchestrator lifecycle execution forwards and persists lease_released through the existing journal adapter path.</done>
  <verify>
    <automated>npx vitest run tests/orchestrator.test.ts tests/orchestrator-journal.test.ts tests/worktree-safety.test.ts tests/recovery.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run check</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lease file -> release operation | Durable local ownership data controls whether source-writing cleanup may release a lease. |
| Release operation -> orchestrator lifecycle | Runtime cleanup evidence crosses from worktree-safety into orchestration control flow. |
| AdvanceResult.events -> JournalAdapter.append | Runtime lease evidence crosses into durable `.planning/orchestration-state.json`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Spoofing | `releaseLeaseOwnership` | mitigate | Verify release ownership against unitId, sessionId, phase, root, branch, host, and pid before emitting `lease_released`. |
| T-11-04-02 | Tampering | lease path resolution | mitigate | Reuse `.planning` path guard and refuse release writes/deletes outside `.planning`. |
| T-11-04-03 | Repudiation | orchestrator release lifecycle | mitigate | Persist `lease_released` through `AdvanceResult.events` -> `JournalAdapter.append` with bounded unit/session/root/branch evidence. |
| T-11-04-04 | Information Disclosure | release journal event | mitigate | Reuse existing journal redaction; include only D-18 bounded fields and no raw env/user text/secrets. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are required; if executor adds one, stop for package legitimacy review. |
</threat_model>

<verification>
Run targeted tests after each task, then run `npm run check` at plan close. Confirm with a source grep that `leaseReleasedEvent` has at least one production caller outside tests and that tests proving runtime persistence do not manually append `leaseReleasedEvent`.
</verification>

<success_criteria>
- A real release operation emits `lease_released` only when the current worker owns the lease.
- Source-writing Unit completion/cleanup forwards `lease_released` through `AdvanceResult.events`.
- `createAutoOrchestrator` persists `lease_released` through the existing journal adapter path.
- Integration coverage proves actual runtime persistence without manually appending a synthetic event.
</success_criteria>

<output>
Create `.planning/phases/11-worktree-safety-recovery-classification/11-04-SUMMARY.md` when done.
</output>
