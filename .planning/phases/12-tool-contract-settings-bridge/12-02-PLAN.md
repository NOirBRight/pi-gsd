---
phase: 12-tool-contract-settings-bridge
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/settings-bridge/types.ts
  - src/settings-bridge/source.ts
  - src/settings-bridge/cache.ts
  - src/settings-bridge/format.ts
  - src/settings-bridge/index.ts
  - src/orchestrator/settings.ts
  - src/orchestrator/index.ts
  - src/orchestrator/trigger.ts
  - src/extension.ts
  - src/gsd-models.ts
  - tests/settings-bridge.test.ts
  - tests/orchestrator-settings.test.ts
  - tests/extension.test.ts
  - tests/gsd-models.test.ts
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
must_haves:
  truths:
    - "SETTINGS-01: GSD-related Pi prompt contexts include a concise effective workflow settings summary, routing summary, source metadata, hash, mtime, and official package version per D-09/D-11/D-12."
    - "SETTINGS-01: Non-GSD Pi conversations do not receive settings noise per D-10."
    - "SETTINGS-02: Settings Bridge reads the same effective source used by upstream `gsd:settings`, including active-workstream config resolution per D-13."
    - "SETTINGS-02: Settings refresh uses lazy mtime/hash checks before GSD context injection or native dispatch and does not use long-lived watchers per D-14."
    - "SETTINGS-02: Pi notifies at most once per newly observed settings hash per D-15."
    - "SETTINGS-02: Settings parse failure blocks GSD context/native auto dispatch with warning/remediation while ordinary non-GSD chat continues per D-16."
  artifacts:
    - path: "src/settings-bridge/index.ts"
      provides: "Settings Bridge API for source resolution, lazy cache refresh, formatted context, and GSD safety decisions"
      exports: ["resolveGsdConfigSource", "createSettingsBridge", "formatSettingsContext"]
    - path: "src/orchestrator/settings.ts"
      provides: "Native orchestrator settings resolver using upstream-compatible source resolution"
      contains: "resolveGsdConfigSource"
    - path: "src/extension.ts"
      provides: "Pi session/context/input hook integration for settings context and notify-once behavior"
      contains: "formatSettingsContext"
    - path: "tests/settings-bridge.test.ts"
      provides: "Source resolution, cache, hash, formatting, parse failure, and notification tests"
    - path: "tests/orchestrator-settings.test.ts"
      provides: "Active-workstream and same-source resolver regression tests"
    - path: "tests/extension.test.ts"
      provides: "GSD-only context injection, notify-once, and native auto parse-failure blocking tests"
    - path: "tests/gsd-models.test.ts"
      provides: "Model/profile routing summary regression tests for Settings Bridge"
  key_links:
    - from: "src/orchestrator/settings.ts"
      to: "src/settings-bridge/source.ts"
      via: "shared upstream-compatible config source resolver"
      pattern: "resolveGsdConfigSource"
    - from: "src/extension.ts"
      to: "src/settings-bridge/index.ts"
      via: "session/context/input hooks refresh cache and format redacted GSD settings context"
      pattern: "createSettingsBridge|formatSettingsContext"
    - from: "src/extension.ts"
      to: "src/orchestrator/trigger.ts"
      via: "native auto handoff checks settings freshness before starting GSD orchestration"
      pattern: "ensureGsdSettingsReady"
---

<objective>
Build the Phase 12 Settings Bridge for source resolution, lazy refresh, Pi prompt context injection, and native auto dispatch safety.

Purpose: SETTINGS-01 and SETTINGS-02 require Pi-visible settings context and native runtime behavior to use the same effective GSD settings source as upstream `gsd:settings`, with concise redacted summaries and conservative failure behavior.
Output: `src/settings-bridge/` module, shared source resolver in orchestrator settings, extension hook integration, model routing summary support, and settings/extension tests.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tool-contract-settings-bridge/12-CONTEXT.md
@.planning/phases/12-tool-contract-settings-bridge/12-RESEARCH.md
@src/orchestrator/settings.ts
@src/orchestrator/types.ts
@src/orchestrator/index.ts
@src/orchestrator/trigger.ts
@src/extension.ts
@src/gsd-models.ts
@tests/orchestrator-settings.test.ts
@tests/extension.test.ts
@generated/prompts/gsd-settings.md
@generated/workflows/workflows/settings.md
@generated/workflows/references/planning-config.md
@.planning/config.json
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED Settings Bridge source, cache, formatting, and extension tests</name>
  <files>tests/settings-bridge.test.ts, tests/orchestrator-settings.test.ts, tests/extension.test.ts</files>
  <behavior>
    - SETTINGS-02/D-13: `resolveGsdConfigSource` follows upstream `gsd:settings` semantics: explicit config path first, `.planning/active-workstream` to `.planning/workstreams/&lt;slug&gt;/config.json` next, `.planning/config.json` next, root `config.json` legacy fallback last.
    - SETTINGS-01/D-09/D-11/D-12: formatted context is a concise effective settings summary with workflow toggles, model/profile routing summary, source path/kind/hash/mtime, and official package/version, not raw config JSON or every available Pi model.
    - SETTINGS-01/D-10: context injection happens only for GSD-related sessions, prompts, workflows, or native auto context.
    - SETTINGS-02/D-14/D-15: lazy mtime/hash refresh happens before GSD context/native dispatch and notifies at most once per newly observed hash.
    - SETTINGS-02/D-16: parse failure blocks GSD context/native auto dispatch with warning/remediation while non-GSD chat continues.
  </behavior>
  <action>Create `tests/settings-bridge.test.ts` and extend `tests/orchestrator-settings.test.ts` plus `tests/extension.test.ts` before production code. Use temp project fixtures with `.planning/config.json`, root `config.json`, `.planning/active-workstream`, and `.planning/workstreams/{slug}/config.json`. Include a malformed JSON fixture to assert GSD context/native auto is blocked and ordinary non-GSD context continues per D-16. Assert formatted context never contains raw JSON braces for full config, full model catalog dumps, `model_overrides` object dumps, tokens, secrets, or unrelated conversation injection. Assert notifications are keyed by newly observed hash and repeated context hook calls do not duplicate notifications per D-15.</action>
  <verify>
    <automated>npx vitest run tests/settings-bridge.test.ts tests/orchestrator-settings.test.ts tests/extension.test.ts --reporter=dot</automated>
  </verify>
  <done>The new/extended tests fail for missing `src/settings-bridge` APIs, missing active-workstream source precedence, missing redacted context injection, missing lazy refresh, and missing parse-failure blocking before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared settings source resolver, lazy cache, and concise formatter</name>
  <files>src/settings-bridge/types.ts, src/settings-bridge/source.ts, src/settings-bridge/cache.ts, src/settings-bridge/format.ts, src/settings-bridge/index.ts, src/orchestrator/settings.ts, src/gsd-models.ts, tests/settings-bridge.test.ts, tests/orchestrator-settings.test.ts, tests/gsd-models.test.ts</files>
  <behavior>
    - SETTINGS-02/D-13: native `resolveWorkflowSettings` and Settings Bridge use the same effective settings source resolver as upstream `gsd:settings`.
    - SETTINGS-02/D-14: cache refresh compares mtime/hash lazily and reparses only when the source changes.
    - SETTINGS-01/D-09/D-11/D-12: formatter summarizes effective workflow settings, model/profile routing, source metadata, resolved hash, mtime, and official package/version without dumping raw config.
    - SETTINGS-02/D-16: parser errors are represented as structured bridge state that GSD callers can block on; defaults are not silently used for GSD execution after a parse failure.
  </behavior>
  <action>Create `src/settings-bridge/` with TypeScript Result-style APIs and NodeNext `.js` imports. Implement `resolveGsdConfigSource({ cwd, configPath })` using explicit path, active-workstream config, project `.planning/config.json`, and legacy root `config.json` fallback semantics from generated `settings.md` per D-13. Update `resolveWorkflowSettings` in `src/orchestrator/settings.ts` to call this shared resolver and include source path/kind metadata in `workflowMetadata` or a typed extension compatible with existing tests. Implement lazy cache refresh in `cache.ts` using file mtime and SHA-256 hash per D-14; include `sourcePath`, `sourceKind`, `hash`, `mtimeMs`, official package name/version, and parse status. Add or update `tests/gsd-models.test.ts` while adding a small `src/gsd-models.ts` summary helper that reads current `model_profile`/tier mapping using existing catalog logic and emits a routing summary without duplicating the router or listing every Pi model per D-11. Implement `formatSettingsContext` to produce concise redacted markdown with effective workflow toggles/defaults, model/profile summary, source metadata, hash, mtime, and official package/version per D-09/D-12.</action>
  <verify>
    <automated>npx vitest run tests/settings-bridge.test.ts tests/orchestrator-settings.test.ts tests/gsd-models.test.ts --reporter=dot</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`resolveWorkflowSettings` and Settings Bridge agree on source precedence, active-workstream config is preferred when present, cache refresh is mtime/hash based, formatted summaries are concise/redacted, and parse failures are structured blockers for GSD callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Settings Bridge into Pi context hooks and native auto handoff</name>
  <files>src/extension.ts, src/orchestrator/index.ts, src/orchestrator/trigger.ts, src/settings-bridge/index.ts, tests/extension.test.ts, tests/settings-bridge.test.ts</files>
  <behavior>
    - SETTINGS-01/D-09/D-10/D-11/D-12: Pi `context` hook injects the formatted settings summary only when messages/session are GSD-related and preserves existing runtime path rewriting.
    - SETTINGS-02/D-14: `session_start`, GSD `context`, and native `input` handoff refresh lazily by mtime/hash rather than using file watchers.
    - SETTINGS-02/D-15: Pi notifications happen at most once per newly observed settings hash and summarize changed source/effective settings change.
    - SETTINGS-02/D-16: malformed settings warn/block GSD context/native auto dispatch but leave unrelated Pi chat on the normal continue path.
  </behavior>
  <action>Instantiate a Settings Bridge cache inside `piGsdExtension`. On `session_start`, parse/cache best-effort but do not inject context yet per D-10; notify only package resolution and any newly observed settings hash once per D-15. In the `context` hook, preserve existing `rewriteMessageForRuntime` behavior and append/prepend the formatted settings context only when `isGsdRelatedContext` detects GSD slash commands, generated GSD workflow references, native auto context, or GSD agent/workflow messages; do not add settings context to unrelated conversations per D-10. In the `input` native auto path, refresh settings before `createNativeAutoHandoff`; if parsing failed, notify warning/remediation, return `{ action: "handled" }` for the GSD native command so it does not silently proceed with defaults, and leave non-GSD input as `{ action: "continue" }` per D-16. If a source hash changes, send exactly one concise notification per hash with source path/kind and effective setting changes per D-15. Add any needed orchestrator dependency hook so native auto uses the same refreshed settings result rather than a divergent resolver.</action>
  <verify>
    <automated>npx vitest run tests/extension.test.ts tests/settings-bridge.test.ts tests/orchestrator-settings.test.ts --reporter=dot</automated>
    <automated>npm run check</automated>
  </verify>
  <done>GSD-related Pi contexts show the effective settings/model/source summary; unrelated contexts stay unchanged; changed settings hash notifies once; malformed settings block GSD native auto/context with warning/remediation; full project check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings file → native runtime | Project/user-controlled JSON affects orchestration Unit inclusion and Pi prompt context. |
| settings summary → Pi prompt context | Runtime-derived settings data becomes LLM-visible context. |
| Pi input hook → native auto dispatch | User slash-command input can trigger orchestration if settings are valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | `src/settings-bridge/source.ts` | mitigate | Use upstream-compatible source resolution per D-13, include source path/kind/hash/mtime per D-12, and test active-workstream precedence. |
| T-12-06 | Information Disclosure | `src/settings-bridge/format.ts` and `src/extension.ts` | mitigate | Inject summary only; omit raw config JSON, every Pi model, secrets, tokens, and model override dumps per D-09/D-11/D-12. |
| T-12-07 | Denial of Service | settings change notifications | mitigate | Notify at most once per newly observed settings hash per D-15. |
| T-12-08 | Elevation of Privilege | native auto dispatch after parse failure | mitigate | Block GSD context/native auto dispatch on parse failure and do not silently fall back to defaults per D-16. |
| T-12-09 | Denial of Service | settings refresh | mitigate | Use lazy mtime/hash refresh checks and no long-lived watchers per D-14. |
| T-12-SC | Tampering | npm installs | accept | No new package installs are planned; Package Legitimacy Audit in 12-RESEARCH.md lists no new packages. |
</threat_model>

<verification>
Run targeted RED/GREEN tests as each task specifies, then run `npm run check`. Confirm source resolution behavior with temp fixtures for explicit config path, `.planning/active-workstream`, `.planning/config.json`, and root `config.json`. Confirm context output is concise/redacted and non-GSD messages are unchanged.
</verification>

<success_criteria>
- SETTINGS-01 covered: Pi prompt context receives concise effective workflow/model/source settings only for GSD-related contexts.
- SETTINGS-02 covered: Bridge reads the same source as upstream `gsd:settings`, refreshes lazily by hash/mtime, notifies once per new hash, and blocks GSD execution on parse failure.
- D-09 through D-16 are implemented exactly as cited in task behavior/action text.
- `npx vitest run tests/settings-bridge.test.ts tests/orchestrator-settings.test.ts tests/extension.test.ts --reporter=dot`, `npm run typecheck`, and `npm run check` pass.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source Type | Item | Coverage |
|-------------|------|----------|
| GOAL | Bridge GSD settings.json/effective config into Pi prompt context | Covered by Tasks 1-3. |
| REQ | SETTINGS-01 | Covered by Tasks 1-3. |
| REQ | SETTINGS-02 | Covered by Tasks 1-3. |
| RESEARCH | Active-workstream source resolution gap between upstream `gsd:settings` and current native resolver | Covered by Tasks 1-2. |
| RESEARCH | Shared resolver for `resolveWorkflowSettings`, prompt context, and native dispatch | Covered by Tasks 2-3. |
| RESEARCH | Lazy mtime/hash cache and notify-once behavior | Covered by Tasks 1 and 3. |
| CONTEXT | D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16 | Covered by explicit D citations in task behavior/action. |
| CONTEXT | Deferred Ideas | None. |
</source_audit>

<output>
Create `.planning/phases/12-tool-contract-settings-bridge/12-02-SUMMARY.md` when done.
</output>
