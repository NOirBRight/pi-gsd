---
phase: 12-tool-contract-settings-bridge
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tool-contract/types.ts
  - src/tool-contract/compile.ts
  - src/tool-contract/snapshot.ts
  - src/tool-contract/validate.ts
  - src/tool-contract/index.ts
  - src/orchestrator/gates.ts
  - src/doctor.ts
  - src/generator.ts
  - src/cli.ts
  - package.json
  - generated/tool-contracts.json
  - tests/tool-contract.test.ts
  - tests/orchestrator.test.ts
  - tests/doctor.test.ts
autonomous: true
requirements:
  - CONTRACT-01
  - CONTRACT-02
must_haves:
  truths:
    - "CONTRACT-01: Each dispatchable Unit type has a generated-first dispatch contract covering prompt obligations, allowed tools, schema enum values, validation requirements, and closeout requirements per D-01/D-03."
    - "CONTRACT-02: Native auto orchestration fails closed before dispatch when a dispatch-critical Unit contract field is invalid, mapping to dispatch-contract-invalid stop per D-05."
    - "Runtime validation uses a stable verified snapshot/hash and does not reparse generated prompt/agent/workflow prose on every dispatch per D-04."
    - "Supplement-only Pi overlays cannot relax upstream allowed tools or policy constraints per D-02."
    - "Contract failure evidence is bounded and structured per D-08."
  artifacts:
    - path: "src/tool-contract/index.ts"
      provides: "Tool Contract public module entry for compile/snapshot/validate APIs"
      exports: ["compileToolContracts", "verifyToolContractSnapshot", "validateUnitToolContract"]
    - path: "generated/tool-contracts.json"
      provides: "Generated stable contract snapshot used by runtime validation"
      contains: "contractHash"
    - path: "src/orchestrator/gates.ts"
      provides: "Pre-dispatch Tool Contract gate integration"
      contains: "dispatch-contract-invalid"
    - path: "tests/tool-contract.test.ts"
      provides: "Contract compiler, snapshot, parity, and overlay tests"
    - path: "tests/orchestrator.test.ts"
      provides: "Fail-closed pre-dispatch gate/no-dispatch regression tests"
  key_links:
    - from: "src/generator.ts"
      to: "generated/tool-contracts.json"
      via: "generateAll writes stable Tool Contract snapshot after prompts/agents/workflows are generated"
      pattern: "writeToolContractSnapshot"
    - from: "src/doctor.ts"
      to: "src/tool-contract/index.ts"
      via: "doctor verifies dispatch-critical parity without blocking prose-only drift"
      pattern: "verifyToolContractSnapshot"
    - from: "src/orchestrator/gates.ts"
      to: "src/tool-contract/index.ts"
      via: "validateToolContract gate calls validateUnitToolContract before prepareUnitRoot"
      pattern: "validateUnitToolContract"
---

<objective>
Build the Phase 12 Tool Contract compiler, snapshot verifier, and pre-dispatch gate.

Purpose: CONTRACT-01 and CONTRACT-02 require native orchestration to compile generated-first Unit contracts and reject invalid dispatch inputs before any Unit dispatch or source-writing root preparation. This plan implements D-01 through D-08 without hand-editing upstream-generated prompt, agent, or workflow inputs.
Output: `src/tool-contract/` module, generated stable snapshot wiring, doctor/generate parity checks, and fail-closed gate tests.
</objective>

<execution_context>
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/workflows/execute-plan.md
@D:/Workstation/pi-gsd/node_modules/@opengsd/get-shit-done-redux/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tool-contract-settings-bridge/12-CONTEXT.md
@.planning/phases/12-tool-contract-settings-bridge/12-RESEARCH.md
@.planning/phases/11-worktree-safety-recovery-classification/11-CONTEXT.md
@.planning/phases/11-worktree-safety-recovery-classification/11-01-SUMMARY.md
@.planning/phases/11-worktree-safety-recovery-classification/11-03-SUMMARY.md
@src/orchestrator/gates.ts
@src/orchestrator/types.ts
@src/orchestrator/dispatch.ts
@src/orchestrator/official-config.ts
@src/doctor.ts
@src/generator.ts
@src/cli.ts
@src/frontmatter.ts
@tests/orchestrator.test.ts
@tests/doctor.test.ts
@generated/agents/gsd-planner.md
@generated/agents/gsd-executor.md
@generated/prompts/gsd-plan-phase.md
@generated/workflows/references/planning-config.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract compiler, snapshot, parity, and gate tests</name>
  <files>tests/tool-contract.test.ts, tests/orchestrator.test.ts, tests/doctor.test.ts</files>
  <behavior>
    - CONTRACT-01/D-01/D-03: `compileToolContracts` reads generated prompts, generated agents, generated workflows, and official schema/config manifests to produce one contract per dispatchable Unit type with dispatch-critical fields only.
    - CONTRACT-01/D-02: a Pi overlay may add Pi runtime metadata but a fixture overlay attempting to remove or relax upstream allowed tools/policy constraints is rejected.
    - CONTRACT-01/D-04/D-06: snapshot verification fails for dispatch-critical drift and returns warning-only evidence for prose/docs drift.
    - CONTRACT-02/D-05/D-07/D-08: `runPreDispatchGates` stops before dispatch for invalid native Unit dispatch inputs, returns `dispatch-contract-invalid`, includes bounded evidence fields, and does not include full prompt text, raw user text, secrets, or unbounded diffs.
  </behavior>
  <action>Create failing Vitest coverage before production code. Add `tests/tool-contract.test.ts` for compiler/snapshot/overlay/parity cases using temp generated fixtures plus existing generated files. Extend `tests/orchestrator.test.ts` with a pre-dispatch invalid-contract case that increments a dispatch counter only if dispatch is reached; assert the counter remains 0 and the gate failure has Phase 11 `dispatch-contract-invalid` action `stop` per D-05. Extend `tests/doctor.test.ts` so doctor fails on dispatch-critical Tool Contract drift and reports prose-only drift as warning text without making doctor fail per D-06. Keep generated artifacts as inputs: tests may create temp fixture generated roots, but must not modify `generated/agents`, `generated/prompts`, or `generated/workflows` by hand per D-01.</action>
  <verify>
    <automated>npx vitest run tests/tool-contract.test.ts tests/orchestrator.test.ts tests/doctor.test.ts --reporter=dot</automated>
  </verify>
  <done>The new/extended tests fail for missing `src/tool-contract` APIs, missing snapshot wiring, and `phase-12-contract-seam` seam behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement generated-first compiler, stable snapshot, and doctor/generate verification</name>
  <files>src/tool-contract/types.ts, src/tool-contract/compile.ts, src/tool-contract/snapshot.ts, src/tool-contract/index.ts, src/doctor.ts, src/generator.ts, src/cli.ts, package.json, generated/tool-contracts.json, tests/tool-contract.test.ts, tests/doctor.test.ts</files>
  <behavior>
    - CONTRACT-01/D-01: contracts are compiled from upstream-derived generated prompts, generated agents, generated workflows, and official schema/config manifests.
    - CONTRACT-01/D-02: overlay fields are supplement-only and cannot replace/relax upstream allowed tools, prompt obligations, schema enum values, validation requirements, or closeout requirements.
    - CONTRACT-01/D-03/D-06: parity checks distinguish dispatch-critical fields from non-critical prose/docs drift.
    - CONTRACT-01/D-04: `generate` writes or verifies a deterministic snapshot and runtime consumers can load by stable hash without reparsing all generated markdown.
  </behavior>
  <action>Create `src/tool-contract/` with typed `ToolContract`, `ToolContractSnapshot`, `ToolContractFailure`, and `ToolContractWarning` shapes. Implement `compileToolContracts` by using existing NodeNext `.js` import style and `splitFrontmatter` patterns to read dispatch targets, generated agent frontmatter `tools`, prompt/workflow existence and obligation markers, official workflow schema keys/enums from `loadOfficialWorkflowConfig`, validation requirements, and closeout requirements. Implement stable JSON serialization and SHA-256 hashing in `snapshot.ts`; include `contractVersion`, `contractHash`, source paths, official package/version, and dispatch-critical fields only. Wire `generateAll`/CLI `generate` to produce `generated/tool-contracts.json` after prompts/agents/workflows are generated, and update `package.json` `files` so the snapshot is published. Extend `runDoctor` to verify the snapshot against current generated inputs; fail on dispatch-critical mismatch per D-03 and report prose/docs drift as warning-only per D-06. Do not hand-edit generated prompt/agent/workflow runtime targets; only the contract snapshot is generated output per D-01/D-04.</action>
  <verify>
    <automated>npx vitest run tests/tool-contract.test.ts tests/doctor.test.ts --reporter=dot</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`npm run generate` produces a deterministic `generated/tool-contracts.json`; doctor can verify it; critical drift fails; warning-only drift does not block; overlay relaxation attempts are rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace pre-dispatch seam with fail-closed Tool Contract gate</name>
  <files>src/tool-contract/validate.ts, src/tool-contract/index.ts, src/orchestrator/gates.ts, tests/orchestrator.test.ts, tests/tool-contract.test.ts</files>
  <behavior>
    - CONTRACT-02/D-05: dispatch-critical failures map to `recoveryDecision.class = "dispatch-contract-invalid"` and `action = "stop"`.
    - CONTRACT-02/D-07: invalid-input rejection covers native Unit dispatch inputs first and does not expand slash-command parser scope except when native handoff would create a Unit contract violation.
    - CONTRACT-02/D-08: failure evidence includes `unitId`, `unitType`, `contractVersion` or `contractHash`, `failedField`, bounded expected/actual summaries, and source paths; it excludes full prompts, full user text, secrets, tokens, and unbounded diffs.
    - CONTRACT-02/D-04: runtime validation loads verified snapshot data and validates the current Unit cheaply.
  </behavior>
  <action>Implement `validateUnitToolContract` as a cheap runtime validator against `generated/tool-contracts.json` or an injected snapshot for tests. Replace `validateToolContract` in `src/orchestrator/gates.ts` so it validates `unit.id`, `unit.type`, `resolveUnitDispatchTarget(unit)`, required prompt/agent target presence, schema enum membership, and dispatch-critical metadata/args obligations before `prepareUnitRoot`. Convert failures into a `GateResult` failure with `reason`, `exitReason`, and `recoveryDecision.class` set to `dispatch-contract-invalid`, `retryable: false`, and bounded evidence per D-08. Preserve existing gate order and success evidence, but remove `phase-12-contract-seam:*` seam behavior. Keep user-facing slash-command parsing unchanged unless `detectNativeAutoTrigger` output would create a contract-invalid Unit per D-07.</action>
  <verify>
    <automated>npx vitest run tests/tool-contract.test.ts tests/orchestrator.test.ts --reporter=dot</automated>
    <automated>npm run check</automated>
  </verify>
  <done>Pre-dispatch Tool Contract validation runs before worktree preparation, invalid Unit dispatch never reaches dispatch, failure classification is `dispatch-contract-invalid -> stop`, and full project check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated artifacts → contract compiler | Upstream-derived markdown/manifests are parsed into runtime enforcement data. |
| Unit metadata → pre-dispatch gate | Native Unit IDs/types/metadata cross into execution control. |
| contract failure → journal/status | Structured evidence is persisted and shown to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `src/tool-contract/compile.ts` / overlay handling | mitigate | Reject overlay attempts to replace or relax upstream-derived dispatch-critical fields per D-02; test attempted allowed-tool/policy relaxation. |
| T-12-02 | Elevation of Privilege | `src/orchestrator/gates.ts::validateToolContract` | mitigate | Fail closed before dispatch and before `prepareUnitRoot`; map to `dispatch-contract-invalid` stop per D-05. |
| T-12-03 | Information Disclosure | contract failure evidence and journal/status fields | mitigate | Emit only bounded `unitId`, `unitType`, hash/version, failed field, scalar expected/actual summaries, and source paths per D-08. |
| T-12-04 | Denial of Service | runtime contract validation | mitigate | Use stable snapshot/hash at runtime rather than reparsing generated prompt/agent/workflow content per D-04. |
| T-12-SC | Tampering | npm installs | accept | No new package installs are planned; Package Legitimacy Audit in 12-RESEARCH.md lists no new packages. |
</threat_model>

<verification>
Run targeted RED/GREEN tests as each task specifies, then run `npm run check`. Confirm `grep -R "phase-12-contract-seam" src tests | grep -v '^#'` returns no production/test seam dependency after Task 3. Confirm `generated/agents`, `generated/prompts`, and `generated/workflows` are used as inputs and not hand-edited runtime targets.
</verification>

<success_criteria>
- CONTRACT-01 covered: one stable generated-first contract exists per dispatchable Unit type with dispatch-critical prompt/tool/schema/validation/closeout fields.
- CONTRACT-02 covered: native orchestration gates dispatch through the contract and rejects invalid Unit inputs before dispatch.
- D-01 through D-08 are implemented exactly as cited in task behavior/action text.
- `npx vitest run tests/tool-contract.test.ts tests/orchestrator.test.ts tests/doctor.test.ts --reporter=dot`, `npm run typecheck`, and `npm run check` pass.
</success_criteria>

<source_audit>
## Multi-Source Coverage Audit

| Source Type | Item | Coverage |
|-------------|------|----------|
| GOAL | Compile per-Unit tool/prompt/policy/schema contract before dispatch | Covered by Tasks 1-3. |
| REQ | CONTRACT-01 | Covered by Tasks 1-2. |
| REQ | CONTRACT-02 | Covered by Tasks 1 and 3. |
| RESEARCH | Generated-first compiler/snapshot from generated agents/prompts/workflows/manifests | Covered by Task 2. |
| RESEARCH | Gate seam in `src/orchestrator/gates.ts` before `prepareUnitRoot` | Covered by Task 3. |
| RESEARCH | Doctor/check parity distinction between critical drift and prose drift | Covered by Tasks 1-2. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08 | Covered by explicit D citations in task behavior/action. |
| CONTEXT | Deferred Ideas | None. |
</source_audit>

<output>
Create `.planning/phases/12-tool-contract-settings-bridge/12-01-SUMMARY.md` when done.
</output>
